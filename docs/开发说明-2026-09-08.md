# txgy-cloud（MES 副本）开发说明

> 总结日期：2026-09-08（最新追加 2026-09-10，见 §8/§9/§10/§11） ｜ 仓库：`D:\txgycloud-dev-mes-project-temp\txgycloud-dev-mes-project-temp`（工作副本，无 .git）
> 当前开发主线：**MES 安全环保（环保污染监控）模块**

---

## 1. 项目概况

txgy-cloud 企业级微服务开发平台（内部代号 OPENLAB BS 项目基础脚手架），基于 Spring Cloud 微服务架构，内置系统管理、多租户、工作流、支付、商城、CRM、ERP、WMS、MES、IoT、IM、AI 大模型等业务模块。

| 项 | 内容 |
| --- | --- |
| 后端 | Java 25（`D:\jdk-25.0.2`）、Spring Boot 4.1.0、Spring Cloud 2025.1.2、Spring Cloud Alibaba（Nacos） |
| ORM/中间件 | MyBatis-Plus 3.5.16、MySQL 8.x、Redis、Nacos |
| 前端 | Vue 3 + Vite（vben 管理后台 web-antd）、uni-app（移动端）、go-view（大屏） |
| 构建 | Maven 3.9.16 多模块（约 200 个模块） |
| 业务模块 | `txgy-module-*` 按 `*-api`（RPC 契约/DTO/枚举）+ `*-server`（Controller/Service/Mapper/DO）分层 |

**目录备忘**（详见 `docs/txgycloud-code-map.md`）：
- 网关：`txgy-gateway`（48080，`/admin-api/**` 静态路由 + Nacos 服务发现转发）
- MES：`txgy-module-mes`（server 入口 `MesServerApplication`）
- 管理后台：`txgy-ui-admin-vben/apps/web-antd`
- 初始化 SQL：`sql/00-sql文件/`、`deploy/mysql/init/`（按编号）、`deploy/mysql/upgrade/`

---

## 2. 本地开发环境与运行拓扑

本机当前验证集合（`deploy/docker-compose.mes.yml`）：

```text
mysql:8.0（root/root，库 ruoyi-vue-pro）+ redis + nacos(:8848)
  └─ gateway(:48080) → system-server → mes-server（容器内 48091）
  └─ 前端 web-antd（dev 端口 5666，需 Node 22.20：D:\txgy-node22）
```

- `dev-logs/` 下 gateway/system/mes 三份日志均运行至 2026-09-07 16:55，服务健康。
- MES 单模块构建：`mvn clean install -pl :txgy-module-mes-server -am`。
- 菜单/路由为数据库驱动：新页面 = 前端代码 + 菜单 SQL（如 `set-pollution-check-20260903-menu.sql`）+ 角色授权。

---

## 3. 当前开发主线：MES 环保污染监控

### 3.1 需求

- 需求文档：`docs/MES环保污染监控需求文档.md`（v0.1，2026-09-03，**状态：待评审**）。
- 业务流程图：仓库上级目录 `环保模块业务流程图.html`（2026-09-07）。
- 定位：在 MES 主流程上以 **AI 初筛 + 人工复核** 双阶段，对生产线物品去向做污染监控与合规管控；**环保判定独立于质量判定**（不触发退货、不改质量口径）。

四个插桩环节（贯穿全链，一条记录可追溯）：

| 环节 | 常量 | 关键规则 |
| --- | --- | --- |
| 采购入库 | `PURCHASE_INBOUND` | IQC 合格后入库前初筛；判有污染≠退货，入受控存储 |
| 生产领用 | `MATERIAL_ISSUE` | 领出前复核，有污染 → 拒绝领用（源头隔离） |
| 中间废弃物 | `WASTE_INTERMEDIATE` | 无污染 → 回用/排放；有污染 → 隔离暂存 |
| 成品 | `FINISHED_PRODUCT` | 达标有污染 → 隔离+标记不出库；不达标 → 整批锁定 |

核心规则：AI 只给三值预分类（无污染/有污染/不确定）；**终态一律人工复核二值收口（CLEAN/POLLUTED）**，不允许"不确定"留存。

### 3.2 已实现功能（后端 `txgy-module-mes-server`）

**污染判定记录** `service/set/pollutioncheck/` + `controller/admin/set/pollutioncheck/`：
- `MesSetPollutionCheckController`：create / update / delete / review / prescreen / get / page / log-list。
- `MesSetPollutionCheckServiceImpl`：
  - 四环节 + 二值复核校验，记录编号 `PC-yyyyMMddHHmmssNNN`；
  - 创建/改单时自动回填 AI 初筛（改单触发 AI 重筛）；
  - 复核时按"环节 × 复核结果"给出默认处置（`NORMAL_INBOUND` / `ISSUE_ALLOWED` / `REJECT_ISSUE` / `REUSE` / `ISOLATE_STORAGE` / `CONTROLLED_STORAGE`）与默认存储方法（无污染→普通仓储；有污染→沿用 AI 推荐，否则受控隔离）；
  - 复核后同事务**写透**（`MesPollutionControlService.applyReviewEffect`）→ 批次污染戳 + 自动登记台账。

**AI 初筛** `MesSetPollutionCheckAiService`：
- **已接真实 DeepSeek 大模型**（`chat/completions`、`response_format=json_object`、temperature 0.2、25s 超时），输出四字段 JSON（aiResult / confidence / reason / suggestedStorage），逐字段清洗兜底；
- 未配置 api-key 或调用失败时**自动降级为污染特征词典**（安全词优先匹配"无铅"先于"铅"，命中污染词如铅镉铬汞苯甲醛等 → POLLUTED；均未命中 → UNCERTAIN，置信度 55–76 随机）；
- 配置项（api-key 仅本地配置，勿提交仓库）：`txgy.pollution-ai.deepseek.api-key / base-url / model`。
- 说明：需求原计划 P1 词典 mock、P2 真实 AI，实际已提前完成 P2 接入（带降级兜底）。

**污染管控写透** `service/pollution/MesPollutionControlServiceImpl`：
- 台账行 = 批次污染管控的权威；批次戳只是"该批是否有未闭环台账行"的投影缓存；
- 复核=有污染 → 校验库位/真实批次（中间废弃物允许空批次）→ 按 `source_check_id` 幂等登记台账（STORED）→ 批次戳投影 `POLLUTED`；
- 复核=无污染 → 解除该批历史未闭环台账行（CLEARED，留痕）→ 清批次戳；
- 台账处置终态（已回用/已排放/已处置）→ 按剩余开放行重投影；
- 门禁：`assertIssueAllowed`（领用拦截）、`assertOutboundAllowed`（出库拦截，POLLUTED 或 marked 即拦）；
- `purgePendingByBizNo(Item)`：单据创建/更新时清理未复核的悬挂判定。

**污染台账** `set/pollutionledger/`：
- 状态机 `STORED(暂存) → PROCESSING(处置中) → {REUSED/DISCHARGED/DISPOSED}`，外加 `CLEARED(自动解除)`；
- 每次流转写台账历史留痕。

**审计履历**（`pollution-audit-20260903.sql`）：
- `mes_pollution_check_log`：CREATE（含 AI 初筛快照）/ UPDATE（重筛快照）/ REVIEW（人工终态快照）各一行，不可改；
- `mes_pollution_ledger_log`：登记/处置中/终态/自动解除每步留痕。

### 3.3 与 WM 主流程的联动（内嵌）

- 后端门禁已织入仓储服务：
  - `wm/itemreceipt`（采购入库）：创建/更新时清理该单悬挂判定；
  - `wm/productissue`（生产领用）：按明细批次逐行 `assertIssueAllowed` 拦截污染批；
  - `wm/productsales`（销售出库）：`assertOutboundAllowed` 拦截污染/标记批；
  - `wm/productreceipt`（产品入库）同样清理悬挂判定。
- 前端共享弹窗 `views/mes/wm/_pollution/judge-modal.vue`，已内嵌到 wm 真实单据页（itemreceipt / productissue / productreceipt / productsales），权限 `mes:set-pollution-check:create`。

### 3.4 前端页面（web-antd）

- `views/mes/safetyEnv/pollutionCheck/`：判定记录列表（index）+ 表单（form，含 AI 预筛回显）+ 人工复核弹窗（review，仅二值）+ 履历抽屉（history-drawer）。
- `views/mes/safetyEnv/pollutionLedger/`：台账列表 + 处置流转弹窗（status-modal）+ 履历抽屉。
- `api/mes/safetyEnv/pollutionCheck|pollutionLedger/index.ts`：API 封装。

### 3.5 数据库变更（`sql/00-sql文件/`，均 2026-09-03）

| 文件 | 内容 |
| --- | --- |
| `set-pollution-check-20260903-p0.sql` | 判定记录表 `mes_set_pollution_check` |
| `set-pollution-check-20260903-menu.sql` / `-cleanup-menu.sql` | 菜单 34301（污染判定，父 34000） |
| `set-pollution-check-20260903-rebind-cleanup.sql` | 判定记录按单据重新绑定清理 |
| `pollution-landing-20260903.sql` | **P2 真落地**：`mes_wm_batch` 加污染戳 4 列 + `mes_pollution_ledger` 台账 + 菜单 34302（台账，复制 34301 授权） |
| `pollution-audit-20260903.sql` | `mes_pollution_check_log` + `mes_pollution_ledger_log` 审计表 |

---

## 4. 联调验证情况（截至 2026-09-07）

- 服务实测：mes-server 运行于容器端口 48091，网关/system/mes 日志至 09-07 16:55 无异常；当日 15:54 实际请求过 `/admin-api/mes/safety-env/pollution-check/get`、`/pollution-ledger/history`（32ms / 41ms）。
- 数据口径：**wm 真实单据全部在租户 1**（租户 2010 无单据），联调均以租户 1 为准。
- 悬挂判定按 `环节 + bizNo` 过滤，随真实单据联动创建/清理。
- 中间废弃物判定挂接生产工单 **MO202504120001**。

---

## 5. 其他并行/历史开发内容

- **HRM 招聘模块第二组**：接口与状态机确认稿、数据与 VO 设计稿、总代码修改记录（`docs/HRM招聘模块第二组*`）。
- **HRM × BPM 请假审批链路**：代码地图第 5–6 节，`BpmProcessInstanceApi` RPC 双向回写。
- **数据**：`deploy/mysql/init/` 编号脚本；`init-gubang-crm-2012.sql`（CRM 初始化）、`wms_item_quality_report_20260903.sql`、`deploy/mysql/upgrade/20260902-mes-pro-project*`（MES 生产项目来源类型扩展）。

---

## 6. 遗留事项与下一步

1. **需求评审**：`MES环保污染监控需求文档.md` 仍为"待评审"状态；评审遗留：判有污染的原料复核后是否允许再次领用（当前默认拒绝）。
2. **成品环节**（FINISHED_PRODUCT）判定与不达标"有害/无害"分支的前端页面需按验收用例补全联调（后端四环节已通）。
3. ~~批次全链追溯待落地~~ → **已完成**（2026-09-10，见 §9.3，追溯页 `batchNo` 聚合）。
4. ~~存储方法字典 / 排放合规流水按 P2 推进~~ → 排放合规流水**已完成**（2026-09-10，见 §9.1）；**存储方法字典经评审决定不建**（AI 建议已覆盖）；待复核队列页面（`todo`）仍为 P2。
5. DeepSeek api-key 为本地配置，正式环境需走配置中心且不入库/不入仓。
6. 本目录为**无 .git 的工作副本**，改动以真实开发仓库为准；推送前按代码地图第 10 节闸门（先本地验证，未经确认不 push）。

---

## 7. 2026-09-09 追加：排污许可模块落成（空壳表转可用模块）

> 团队质优设计文档：`docs/MES环保模块质优化设计-团队终版-2026-09-09.md`（按证排污为设计基准源之一）。本期将 09-02 创建的**空壳表** `mes_set_pollution_permit`（原 3 行 stub 数据、无任何代码）落地为可增删改查 + 列表检索的可用主数据模块。

**后端**（`txgy-module-mes-server`，包 `set/pollutionpermit`）：
- Controller `/mes/safety-env/pollution-permit`：create/update/delete/get/page，权限 `mes:set-pollution-permit:{create,update,delete,query}`。
- Service 校验：证号全库唯一（跨租户由录入方加 `-H` 后缀）、有效期止≥始、状态枚举 `ACTIVE/EXPIRED/REVOKED`、**排放口必填**（按证排污最小抓手）、`annualLimits` JSON 合法性。
- 错误码段 `1_040_820_000+`（ErrorCodeConstants，API 模块）。
- 构建坑：Windows 下 spring-boot repackage 对**运行中进程锁定的 jar** 报 rename 失败 → 必须先停 mes-server 再 `mvn -P '!jdk-17' package`；`-o` 离线会静默跳过 repackage 产出 thin jar。运行建议直接 `mvn install` 后从 `~/.m2` 的 fat jar 启动（规避 D 盘目标文件偶发不可读）。

**前端**（web-antd `views/mes/safetyEnv/pollutionPermit/`）：index.vue（VxeTable 列表 + 状态 Tag）+ modules/form.vue（新建/编辑弹窗）+ data.ts + api 封装。

**菜单/授权**：`sql/00-sql文件/set-pollution-permit-20260909-menu.sql` → 功能菜单 **34350**（排污许可证，父 34000，sort 192）+ 按钮 34360–34363；role_menu 已绑定租户 2010 角色 60185（tenant_admin）。
- **权限缓存坑**：菜单/授权走**裸 SQL** 会绕过 `@CacheEvict`，system-server 的 Redis 权限缓存残留空绑定（`menu_role_ids:2010:34360`、`permission_menu_ids:*`）→ 改完需手动 `redis-cli DEL` 对应 key；mes-server 侧另有 1 分钟 Guava `(userId,permission)` 缓存，改授权后需等其过期或重启。

**种子数据归一**：09-02 stub 行状态 `VALID/EXPIRING` 不在代码枚举内 → 已统一为 `ACTIVE`；`PERMIT-SMOKE-1`（租户 1）企业名乱码已修为「冒烟测试企业」；租户 2010 两行真实数据为华翰智能制造（北京）有限公司 P-110108-2026-0001/0002（0002 备注临近换证）。

**接口实测**（经网关 48080，租户 2010 huahan/admin123）：page 返回 2 行 → create 新建 P-110108-2026-0003（id=4）→ get 回读 → update 置 REVOKED → delete → page 回 2 行；重复证号/非法状态/缺排放口分别报 1040820001/0002/0004。前端三文件经 Vite dev(5666) transform 无报错。

**遗留**：在线监测比对/总量红线/执行报告自动汇总（消费 `annualLimits`/`annualReports`）为后续迭代，本期仅主数据维护。

---

## 8. 2026-09-10 追加：追溯链一期B 落成（复核收口 + 台账处置自动留痕）

> 一期A（污染判定记录四环节 + 台账登记）打通后，复核/处置产生的结论与状态流转此前只散落在各业务表，缺少"一条单号看全生命周期"的只读视图。一期B 只增不改地沉淀 3 张追溯表，并在既有复核/流转接口内**同事务自动插桩**，不新增人工操作入口。

**表结构**（`sql/00-sql文件/pollution-trace-20260909-ddl.sql`，通用 7 列 + tenant 隔离）：
- `mes_set_trace_chain` 追溯链节点：`trace_code`(追溯对象码，现=判定 recordNo)/`trace_type` CHECK|LEDGER/`biz_type`/`biz_no`(=来源判定 recordNo，CHECK 与 LEDGER 共享同键)/`node_stage`(环节或 DISPOSAL)/`node_action`(中文动作，时间轴主文案)/`batch_status`(节点时刻对象状态)/`operator_name`/`node_time`/`extra`；索引 idx_trace_code、idx_biz(biz_type,biz_no)、idx_node_time，Mapper 按 node_time,id 倒序。
- `mes_set_sign_record` 签字记录（复核收口自动留档）。
- `mes_set_weigh_record` 称重记录（**本期仅表+只读页**，待电子秤直采接入）。

**后端**（`txgy-module-mes-server` 包 `set/{tracechain,signrecord,weighrecord}`）：三表各 DO/Mapper/Service(Impl)，Controller 只读分页：
- `/mes/safety-env/pollution-trace/page`（按 traceCode/bizNo 模糊 + bizType/nodeStage 等值）、`/pollution-sign/page`、`/pollution-weigh/page`，权限均为 `mes:set-pollution-trace:query`。
- Controller 直接返回 DO（无 RespVO，只读查询故意精简，省 9 个 VO 文件）。

**自动插桩**（两条既有 Service 改动，@Transactional 同事务）：
- `MesSetPollutionCheckServiceImpl.reviewPollutionCheck`：复核收口后写 CHECK 追溯节点 + REVIEWER 签字（location/opinion 落 sign，extra 落 location）。
- `MesPollutionLedgerServiceImpl.updateLedgerStatus`：每步处置流转（暂存→处置中→已回用/排放/处置）写 LEDGER/DISPOSAL 追溯节点，`node_action="台账状态：暂存 → 处置中"` 形式，`trace_code=biz_no=台账来源判定 recordNo`，与 CHECK 节点串成同一条链。

**菜单/授权**（`pollution-trace-20260909-menu.sql`）：功能菜单 **34370「污染追溯」**（父 34000，sort 193，路径 pollutionTrace，组件 MesPollutionTrace）+ 按钮 **34371「查询」**；role_menu 绑定租户 2010 角色 60185。裸 SQL 改菜单后需清 system-server Redis 权限缓存（见 §7 缓存坑）。

**前端**（web-antd `views/mes/safetyEnv/pollutionTrace/`）：index.vue 只读列表（VxeTable，筛选 traceCode/bizNo/bizType/nodeStage，节点时间倒序）+ modules/trace-drawer.vue 全生命周期抽屉（antd Tabs：追溯链 Timeline + 签字记录表 + 称重记录表，按行 bizNo 拉同键整链，时间轴正序展示）。data.ts 复用 pollutionCheck 的 REVIEW_RESULT_MAP、pollutionLedger 的 LEDGER_STATUS_MAP，统一 `nodeStatusMeta` 决定状态 tag。该抽屉另已复用接入污染判定列表与台账列表的操作列（「追溯」按钮，权限 `mes:set-pollution-trace:query`；判定页仅对已复核行展示），任一页面点开即以该行 recordNo/sourceRecordNo 为键拉整链。

**接口实测**（经网关 48080，租户 2010 huahan，判定 PC-20260907101620002 废切削液全链）：
1. 复核 POLLUTED → trace 增 CHECK 节点、sign 增 REVIEWER(刘洋) 签字、ledger_log 自动登记(暂存)。
2. 台账 暂存→处置中→已处置 两步流转 → trace 各增一条 LEDGER/DISPOSAL 节点，三节点同 bizNo 成链。
3. 三只读页/`get` 均 code 0；weigh 恒空（设计如此）。

**坑**：Windows shell(GBK) 经 curl `--data` 内联提交中文会损坏入库（存 U+FFFD）→ 中文 JSON 需先 Write UTF-8 文件再 `curl --data @file`（修复脚本 `sql/00-sql文件/fix-pollution-trace-e2e-text.sql`）。

**遗留**：称重直采/AUTO 来源、CHECK 上链前端入口（当前列表仅查全链，判定动作仍走原复核页）为后续；本次 e2e 闭环消费了演示判定 PC-20260907101620002，但 2010 租户仍有 11 条待复核在库（复刻可跑 `seed_pollution_2010.sql`）。

---

## 9. 2026-09-10 追加：排放合规流水 + 被标记品筛选 + 批次全链聚合追溯

> 需求文档里「应做」的三项补齐。仍守既有边界：**不建**存储方法字典（AI 建议已覆盖）、**不拆**四个分环节独立页面（保留泛化列表）、**不做**在线监测对接与政府申报接口。`mes_set_weigh_record` 继续只读（无电子秤直采）。

### 9.1 排放合规流水（新表 + 新页）
台账流转到 **已排放(DISCHARGED)** 时，在同一事务里自动落一条排放合规流水（`MesPollutionLedgerServiceImpl.updateLedgerStatus` → `dischargeRecordService.createDischargeRecord`）。

- 表 `mes_set_pollution_discharge`（`sql/00-sql文件/pollution-discharge-20260910-ddl.sql`）：ledger_id / source_record_no / stage / batch_no / item_code / item_name / item_spec / **destination 去向** / **standard 执行标准** / discharge_time / remark + 通用 7 列；索引 idx_source_record_no、idx_ledger_id、idx_discharge_time。
  - 命名说明：需求文档写的是 `mes_pollution_discharge`，本仓 SET 家族统一 `mes_set_*`，故取 `mes_set_pollution_discharge`。
- 去向取值：弹窗登记优先，**留空回退台账 location**（`StrUtil.isNotBlank(reqVO.getDestination()) ? ... : ledger.getLocation()`）。
- 后端包 `set/dischargerecord`：DO/Mapper(`eqIfPresent` sourceRecordNo/stage/batchNo + `likeIfPresent` itemName/destination，dischargeTime,id 倒序)/Service(Impl)/PageReqVO/Controller，接口 `GET /mes/safety-env/pollution-discharge/page`，权限 `mes:set-pollution-discharge:query`（**只读，无手改入口**）。
- 前端：`views/mes/safetyEnv/pollutionDischarge/`（VxeTable 只读页）+ 台账流转弹窗 `status-modal.vue` 在选「已排放」时展开 `destination`/`standard` 两个下拉受控字段（`dependencies.triggerFields:['status']`，仅 DISCHARGED 显示），`api/.../pollutionLedger/index.ts` 的 `StatusPayload` 同步加两字段。
- 菜单 `sql/00-sql文件/pollution-discharge-20260910-menu.sql`：功能菜单 **34380「排放合规流水」**（父 34000，sort 194，path pollutionDischarge，组件 MesPollutionDischarge）+ 按钮 **34381「查询」**；绑角色 60185；裸 SQL 后需清 system-server Redis 权限缓存。

### 9.2 被标记品清单（以台账筛选实现，不新建页面）
仓库里「被标记品禁止出库/领用」的拦截早已在 wm 侧存在（`assertOutboundAllowed` productsales:220、`assertIssueAllowed` productissue:180），缺的只是**看得到**。做法是台账列表加一个筛选，不新增清单页：

- 后端 `MesPollutionLedgerPageReqVO` + `marked` (Boolean)，Mapper `.eqIfPresent(MesPollutionLedgerDO::getMarked, ...)`。
  - 注意 Hutool `ObjectUtil.isNotEmpty(Boolean.FALSE)` 为 **true**，所以 `eqIfPresent(field, false)` 会真的加上 `marked=0` 条件（不会把 false 当空值吞掉）。
- 前端台账搜索表单加「标记品」Select（`true`=仅看被标记品 / `false`=仅看未标记）+ 列表加「标记品」列 Tag（橙色 已标记）。
- 需求文档里「再次领用规则」属评审决策，代码现状默认拒绝（走 `assertIssueAllowed`），未额外加配置项。

### 9.3 批次全链聚合追溯
原追溯页只能按单号（判定 recordNo）查链，跨环节的一批物料看不全。补齐方式：

- `MesSetTraceChainPageReqVO` + `batchNo`；追溯 Mapper 新增 `selectPageByBizNos(Collection<String> bizNos, PageParam)`（`.in(biz_no, bizNos)`）。
- `MesSetPollutionCheckMapper.selectRecordNoListByBatchNo(batchNo)`：先按批次捞出该批**全部判定 recordNo**（去重、非空），再一次性拉这批单号下的所有追溯节点。
- Service 分支：`batchNo` 非空 → 走批次聚合；该批无判定则 `PageResult.empty()`（不落空 IN 查询）。
- 追溯页搜索表单加「批次号」输入（前缀排序在 traceCode 之前）。

### 9.4 接口实测（经网关 48080，租户 2010 huahan）
1. **排放合规流水**：复核 `PC-20260903163248987-H`(含铅焊锡丝 POLLUTED) → 台账 1015 → 流转「已排放」（去向=危废处置单位·东方环保，标准=GB18597-2023）→ 流水页 `code 0 total 2`，两条 destination/standard 中文均正常，登记人 **刘洋**。
2. **被标记品筛选**：`marked=true → total 9`、`marked=false → total 2`。
3. **批次聚合追溯**：`batchNo=RAW-BATCH-001 → total 2`，返回同一业务键下 **CHECK「复核收口：有污染受控」+ LEDGER/DISPOSAL「台账状态：暂存 → 已排放」** 两节点（真正的跨环节链）；`batchNo=NO_SUCH_BATCH → total 0`。
4. 当前演示存量：2010 待复核 **11**、台账总行 12（开放行 0）、排放流水 2、追溯节点 6。

### 9.5 本轮踩到的坑
- **Lombok `@Builder` 不覆盖父类字段**：`MesSetDischargeRecordDO` 继承 `BaseDO`，`builder().creator(...)` 编译不过（`找不到符号 creator`）。改为 build 完再 `record.setCreator(...)`。
- **creator 自动填充只在为空时生效**：`DefaultDBFieldHandler.insertFill` 仅当 creator 为空才填登录用户 id，所以「build 后显式 setCreator(昵称)」能存活到入库——这正是登记人想落**昵称**（而非 80370）的实现方式。首条流水曾落成 id，已由 `sql/00-sql文件/fix-discharge-creator-1013.sql` 修正；第二条经真机验证由代码直接写入昵称（HEX `E58898E6B48B`）。
- 中文仍须 UTF-8 文件 + `curl --data @file`（§8 同坑，本轮所有 body 走文件：`review-1039-polluted.json`、`flow-discharged-1015.json` 等）。

---

## 10. 2026-09-10 第二轮：需求文档真缺口补齐（受控存储 / 待复核队列 / 环保专员终审 / 称重手工登记）

承接 §9，把需求文档里「写了、代码没有」的四项补齐，并把半成品的尾巴收掉。原则同前：**能复用就不新建**（不新开库位字典、不新开清单页、不新开按钮页）。

### 10.1 受控存储（需求 5.4「库位需有污染管控属性标识、普通仓与受控仓隔离防混放」）

不新建库位字典，直接给既有 wm 库位主数据加一个布尔属性：

- DDL `sql/00-sql文件/pollution-controlled-area-20260910-ddl.sql`：
  - `mes_wm_warehouse_area` + `pollution_control tinyint(1) NOT NULL DEFAULT 0`（在 `allow_batch_mixing` 之后）；
  - `mes_set_pollution_check` / `mes_pollution_ledger` 各 + `location_id bigint NULL`（在 `location` 之后）——`location` 继续存**名称快照**，`location_id` 存主数据外键，二者并存便于老数据兼容；
  - 租户 2010 种子：仓 `WH-HZ-RAW` 原料仓 / `WH-HZ-HAZ` 危废暂存仓 → 库区 `LOC-HZ-RAW-A` / `LOC-HZ-HAZ` → 库位 `AREA-HZ-RAW-A-01`(普通) 与 `AREA-HZ-HAZ-01/02/03`(受控，名称刻意对齐历史演示文案「危废暂存间A-01/A-02/C-03」)；租户 1 把 `AREA-FIN-A-01` 置为受控。
  - **回填坑**：判定/台账的 `location` 列是 `utf8mb4_0900_ai_ci`，而 wm 主数据是 `utf8mb4_unicode_ci`，按名称 JOIN 会报 `ERROR 1267 Illegal mix of collations`，必须写 `a.name COLLATE utf8mb4_0900_ai_ci = c.location`。
- 后端门禁落在 `MesPollutionControlServiceImpl.applyReviewEffect`（复核写透的唯一入口，所有复核路径都过这里）：先由 `locationId` 取库位主数据，取不到 → `…_LOCATION_NOT_FOUND`；**有污染**必须给 `locationId` 且库位必须是污染管控库位（否则 `…_LOCATION_ID_REQUIRED` / `…_LOCATION_NOT_CONTROLLED`）；**无污染**不得占用受控库位（`…_LOCATION_CONTROLLED_FORBIDDEN`）。校验在同一事务内，抛错即整体回滚。
- 库位名称以主数据为准**回写快照**（判定行与台账一致）。
- 前端：库位下拉复用既有 `views/mes/wm/warehouse/components/area-select.vue`，仅加一枚「受控库位」橙色 Tag；库位主数据页 `views/mes/wm/warehouse/area/` 加「污染管控」Switch + 列表列 Tag；复核弹窗 `location` 改为 `locationId`（选择器），搜索表单加「判定状态」。

### 10.2 待复核队列（需求 5.5-6）

后端其实早已具备：`MesSetPollutionCheckPageReqVO.reviewed` + Mapper 里 `null=全部 / true=已复核(isNotNull review_result) / false=待复核(isNull review_result)`。本轮只补前端：判定页搜索表单加「判定状态」Select（`data.ts` 的 `REVIEWED_OPTIONS`）。**不新建队列页**。

### 10.3 环保专员角色 + 标记品终审（需求 5.6）

- 菜单/角色裸 SQL `sql/00-sql文件/pollution-env-specialist-20260910-menu.sql`：按钮菜单 **34382「标记终审」**（`mes:set-pollution-mark:update`，父 34302）；角色 **60466 环保专员 / `env_specialist`**（租户 2010，data_scope 1）；用户 `huahan_huanbao`（昵称 **郑洁**，密码/部门/岗位/性别/头像从 80370 复制）并绑 9 条菜单（34000/34301/34302/34370/34380/34311/34371/34381/34382）。
- 后端 `MesPollutionLedgerServiceImpl.updateLedgerMark`（`@Transactional`）：幂等（标记值与现态相同直接返回），改 `marked` + updater，落一条台账流转日志（`from_status = to_status = 当前态`，备注「标记品终审：…」）、落一条追溯节点（`LEDGER/DISPOSAL`），最后 `refreshBatchStamp(batchNo)` 重投影批次污染戳。
- 接口 `PUT /mes/safety-env/pollution-ledger/mark`，权限 `mes:set-pollution-mark:update`。
- **职责分离（刻意）**：环保专员**没有** `review`/流转权限，复核归质检、标记终审归环保专员；反过来企业超级管理员 60185 **没授**新权限，本轮实测其调用 `/mark` 返回 403。

### 10.4 称重手工登记（收 §8 遗留「称重无直采」的尾巴）

电子秤未联网前，称重记录不可能为空——补一个手工登记入口：

- `MesSetWeighRecordServiceImpl.createWeighRecord`：净重由服务端按 `毛重 − 皮重` 计算（皮重缺省 0），`净重 <= 0` → `MES_SET_WEIGH_NET_WEIGHT_INVALID`；落库 `data_source = MANUAL`、`operator_name = 当前登录昵称`、`weigh_time` 缺省 now。AUTO 直采通道留待硬件接入。
- 接口 `POST /mes/safety-env/pollution-weigh/create`。**权限刻意复用** `mes:set-pollution-check:create`——手工登记入口挂在追溯抽屉的「称重记录」Tab 内，没有独立页面可挂菜单按钮，为此新造权限只会多一份没人维护的配置。
- 前端 `views/mes/safetyEnv/pollutionTrace/modules/weigh-modal.vue`（Vben Form Modal，含净重实时预览）+ 抽屉里的「手工登记称重」按钮，自动带上当前抽屉的业务单号/类型。

### 10.5 接口实测（经网关 48080，租户 2010；服务从 `~/.m2` fat jar 重启）

| 场景 | 期望 | 实测 |
| --- | --- | --- |
| 待复核筛选计数 | 与库内一致 | DB `9/23` ↔ API `reviewed=false → 9`、`reviewed=true → 23` |
| 有污染复核 **不给库位** | 拒绝 | `1040818008` 有污染判定必须选择污染管控库位 |
| 有污染复核 + **普通库位 727** | 拒绝 | `1040818010` 库位「原料A-01」不是污染管控库位 |
| 有污染复核 + **受控库位 728** | 通过 | `code 0`；台账 1016 落 `危废暂存间A-01 / location_id 728 / STORED` |
| 无污染复核 + **受控库位 728** | 拒绝 | `1040818011` 无污染复核不得占用污染管控库位 |
| 无污染复核 + 普通库位 727 | 通过 | `code 0`，判定行 `location` 快照回写为「原料A-01」 |
| 环保专员 `/mark` | 通过 | `code 0`；台账 `marked=0`、日志行 + 追溯节点「标记品终审：解除标记」操作人 **郑洁** |
| 环保专员 `/review` | 403 | `没有该操作权限` |
| 超管 `/mark` | 403 | `没有该操作权限` |
| 称重登记 `120.5 − 20.5` | 净重 100 | `MANUAL / 净重 100.000 / 刘洋 / 陕A12345` |
| 称重 `10 − 20` | 拒绝 | `1040821000` 净重必须大于 0 |
| 称重缺毛重 | 拒绝 | `400 毛重不能为空` |
| 排放**去向留空** | 回退台账库位 | 流水 destination = 「危废暂存间A-01」（来自 ledger.location），standard 留空 |
| 排放标准留空 | 不回退 | 保持 NULL（只有 destination 有回退语义） |

### 10.6 本轮修掉的自身缺陷

- **`location` 快照漏写**：`review()` 的 `updateById(updateObj)` 只写了前端传来的 `locationId`，而「按库位主数据解析名称」发生在之后的 `applyReviewEffect` 里，解析结果只改了内存 DO 没落库 → 判定行的库位列/追溯节点 `extra` 一直是空，与台账不一致。修法是在 `applyReviewEffect` 解析出名称后补一次窄更新（`LambdaUpdateWrapper` 只 set `location`），一行带注释。修完实测 1057 落「原料A-01」；修前那一行（1055）已用 UPDATE 对齐台账。
- 教训：**写透链路里「在内存里改 DO」不等于「落库」**，凡是 `applyReviewEffect` 之后再无 update 的字段都要单独确认。

### 10.7 本轮遗留

1. 电子秤直采（AUTO）通道未接，`createWeighRecord` 只服务 MANUAL；接硬件时复用同一张表，`data_source` 区分即可。
2. 手工登记复用 `mes:set-pollution-check:create`；若后续把称重做成独立页面，再单独拆权限菜单。
3. 受控库位门禁是**硬门禁**：租户若一个受控库位都没维护，「有污染」复核会直接失败。当前 2010 有 3 个受控库位、租户 1 有 1 个。
4. 菜单/角色是裸 SQL 插入，绕过 `@CacheEvict`：变更后需清 system-server 的 `menu_role_ids:*`、`permission_menu_ids:*`，且 mes-server 侧 Guava 权限缓存 1 分钟（重启即清）。

## 11. 2026-09-10 第三轮：成品不达标三分支 + 判定表导出与筛选补全（需求文档 §11 用例5）

承接 §10。本轮处理需求文档里最后两处「写了、代码没有 / 半成品」——**§11 验收用例5「不达标成品整批锁定不出库」**与**判定表只能翻页不能导出/筛不全**。原则不变：与既有二值不变量正交、复用既有载体、不新造表。

### 11.1 建模口径（为什么不是「把 review_result 改成三分支」）

最容易走偏的一步是直接把 `review_result` 从二值扩成三分支。那样会打破 §9 已经立起来的 R2 不变量——**复核终态二值收口（无污染/有污染），不存在 UNCERTAIN 之类的中间态**，而且「有污染」是驱动台账登记/批次污染戳的唯一判据，混进「返工」会让台账链路失去语义。

所以拆成两个**正交**字段：

| 字段 | 语义 | 取值 | 谁填 |
| --- | --- | --- | --- |
| `review_result` | **污染态**——放行不放行 | `CLEAN` / `POLLUTED`，终态二值 | 四个环节都填 |
| `finished_result` | **达标分支**——成品行不行 | `QUALIFIED` / `REWORK` / `SCRAPPED` | **只有** `FINISHED_PRODUCT` 填 |

- `QUALIFIED` 达标 → 正常入库出厂；
- `REWORK` 局部缺陷 → 返工（补口/重新发泡），**不锁批**；
- `SCRAPPED` 整体报废 → **整批锁定不出库**；有害走受控存储 + 台账，无害直接存储 + 标记。

DDL `sql/00-sql文件/finished-result-20260910-ddl.sql`：`mes_set_pollution_check` 加 `finished_result varchar(32) NULL`（在 `review_result` 之后），存量 12 条成品判定回填 `QUALIFIED`（不改污染态）。实测回填后 `FINISHED_PRODUCT/NULL` 归零，其余环节全 `NULL`。

### 11.2 整批锁定的载体：复用批次污染戳，不新建列

「锁定」需要一个所有出库路径都认的地方。既有 `mes_wm_batch.pollution_marked` 已经被两个门禁读（`MesWmProductIssueServiceImpl:180` 领用、`MesWmProductSalesServiceImpl:220` 出库），直接把报废锁语义并进它——**零新列、零新门禁**。

`MesPollutionControlServiceImpl.refreshBatchStamp` 是批次戳的唯一投影点，改动只有一处：

```java
// 该批存在"整体报废"判定即锁死。与台账无关——无害报废没有污染台账行，
// 也必须挡住出库/领用，且不因后续无污染复核被解除。
boolean scrapLocked = pollutionCheckMapper.selectCount(new LambdaQueryWrapperX<MesSetPollutionCheckDO>()
        .eq(MesSetPollutionCheckDO::getBatchNo, batchNo)
        .eq(MesSetPollutionCheckDO::getFinishedResult, FINISHED_SCRAPPED)) > 0;
```

再把 `scrapLocked` 或进两个分支的 `marked`（`open.isEmpty()` 的清戳分支也要带上，否则无害报废的锁会被清掉）。领用门禁 `assertIssueAllowed` 同时放宽到与出库同口径——`POLLUTED 或 marked` 都拒，原先只看 `POLLUTED`。

**为什么锁定不能挂在台账上**：无害报废不产生污染台账行（`review_result = CLEAN` 时不登记），台账里根本没有东西可挂；而「整体报废」是成品自己独立的结论，与污染态无关。

### 11.3 校验（`reviewPollutionCheck`，全部在写库之前，抛错即整体回滚）

| 错误码 | 码值 | 触发 |
| --- | --- | --- |
| `SET_POLLUTION_CHECK_FINISHED_RESULT_REQUIRED` | `1040818012` | 成品环节没给达标分支 |
| `SET_POLLUTION_CHECK_FINISHED_RESULT_INVALID` | `1040818013` | 分支值不在三个枚举里 |
| `SET_POLLUTION_CHECK_FINISHED_RESULT_NOT_ALLOWED` | `1040818014` | **非**成品环节却传了达标分支 |
| `SET_POLLUTION_CHECK_SCRAP_LOCATION_REQUIRED` | `1040818015` | `SCRAPPED` 却没给存放去向 `locationId` |

`marked` 的计算也一并收紧：报废一律强制 `true`，其余沿用入参或「有污染即标记」。

### 11.4 判定表导出 + 筛选补全（需求文档「判定表要能交出去给环保检查」）

- **后端**：`GET /mes/safety-env/pollution-check/export-excel`，走仓库既有 `export-excel` 套路（`setPageSize(PAGE_SIZE_NONE)` → `ExcelUtils.write` + `@ApiAccessLog(operateType = EXPORT)`）。**权限刻意复用 `:query`**——为导出单开菜单行要多一次裸 SQL + Redis 清缓存，收益不成比例。
- **筛选补全**：`MesSetPollutionCheckPageReqVO` / Mapper 加 `finishedResult`（精确）、`reviewBy`（精确）、`createTime`（区间，`LocalDateTime[]` + `@DateTimeFormat`）；前端搜索表单同步加四项（判定状态、复核分类、达标分支、复核人、创建时间区间）。
- **前端**：`data.ts` 加 `FINISHED_RESULT_OPTIONS` / `FINISHED_RESULT_MAP` + 列表列 + 搜索项；`index.vue` 加导出按钮（`ACTION_ICON.DOWNLOAD` + `downloadFileFromBlobPart`）。
- **复核弹窗**：加隐藏 `stage` 字段（`triggerFields: ['']`，本仓库 `form/category` 等 20+ 处的既有写法）作为 `dependencies` 触发器，`finishedResult` 的 `show` / `required` / `rules` 都随 `stage === 'FINISHED_PRODUCT'` 走。
  - **坑**：`show` 只是 `v-show`（`form-field.vue:341`），字段**仍在校验树里**——所以非成品环节必须用动态 `rules` 把必填摘掉（返回 `null`，`FormSchemaRuleType` 接受 `null` 不接受 `undefined`），光靠 `show` 会把别的环节卡死在提交上。

### 11.5 实测（经网关 48080，租户 2010；服务从 `~/.m2` fat jar 重启）

驱动脚本 `D:\txgycloud-dev-mes-project-temp\e2e\run_e2e.py`（只用 stdlib，含幂等前置清理，可重复跑）：**26 项断言全绿**。

| 用例 | 期望 | 实测 |
| --- | --- | --- |
| A 成品 `CLEAN + QUALIFIED`（批 1003） | 不锁批 | 戳全 `NULL` ✅ |
| B 成品 `POLLUTED + SCRAPPED + 受控库位728`（批 1010） | 锁批 + 落受控库位 | `POLLUTED / marked=1 / 危废暂存间A-01` ✅ |
| C 成品 `CLEAN + SCRAPPED + 普通库位727`（批 1004） | **无害报废也锁批** | `status=NULL / marked=1` ✅ |
| D 同批再来一次 `CLEAN + QUALIFIED` | 报废锁定**不得**被解除 | `marked=1` 仍在 ✅ |
| E1 非成品（`MATERIAL_ISSUE`）传 `finishedResult` | 拒 | `1040818014` ✅ |
| E2 成品缺 `finishedResult` | 拒 | `1040818012` ✅ |
| E3 `SCRAPPED` 缺 `locationId` | 拒 | `1040818015` ✅ |
| E4 分支值非法 | 拒 | `1040818013` ✅ |
| E 被拒后判定行状态 | 仍待复核（回滚干净） | `NULL` ✅ |
| F1~F4 `finishedResult`/`reviewResult`/`reviewBy`/`createTime` 筛选 | 过滤生效且计数自洽 | 全绿 ✅ |
| F5 导出 | 真 Excel 二进制 | HTTP 200 / 10728B / magic `504b0304` ✅ |
| F6 导出表头 | 22 列中文齐全、含「达标分支」、**不泄露** `locationId` | ✅ |
| F7 导出范围 | 全量（不受 `pageSize` 限制） | 导出 37 行 = DB 37 行 ✅ |

前端 `vue-tsc --noEmit` 对 `pollutionCheck` 目录**零新增错误**（仅剩 `index.vue:160` 的 `STAGE_MAP[row.stage]`，属仓库既有写法问题，同 `pollutionLedger`/`pollutionPermit`/`pollutionTrace` 共 7 处）；四个改动文件经 Vite dev(5666) 转换均 200 无报错。

### 11.6 本轮修掉的自身缺陷

- **`@ExcelIgnore` 缺失导致内部字段外泄**：原以为「只要有一个 `@ExcelProperty`，EasyExcel 就只写带注解的字段」——实测**不成立**，导出多出一列英文表头 `locationId`（库位主数据外键，不该给环保检查的人看）。加 `@ExcelIgnore` 修掉，并把「表头逐列核对 + 断言 `locationId` 不存在」固化成 F6 断言，防止回归。
- **e2e 用例跨轮次污染**：用例 A 原先写在批 1010 上，第二轮跑时该批已被上一轮的 `SCRAPPED` 锁死，A 必然失败。批次污染戳是**跨调用残留的状态**，脚本必须在开头复位（现加 `reset()`：清 `PS-E2E-%` 判定行 + 复位 1003/1004/1010 三批的戳）。

### 11.7 本轮遗留

1. **门禁按批次戳断言，未另起一张 APPROVED 领料单**：`productissue`/`productsales` 两个门禁读的就是 `mes_wm_batch.pollution_*`（`…IssueServiceImpl:180` / `…SalesServiceImpl:220`，谓词 `POLLUTED || marked`），本轮直接断言戳本身。租户 2010 目前**没有任何** `mes_wm_product_issue` 单据，要跑真单据级 e2e 得先造 line + 库存 + 虚拟线边库，为一条两条件的 OR 不值当。
2. **演示数据**：批 1004（`PC202600002`）与 1010（`PC202600003`）现处于锁定态——1004 是「无害报废只锁不污染」（`status=NULL / marked=1`），1010 是「有害报废」（`POLLUTED / marked=1 / 危废暂存间A-01`）。这两个正是新功能的活演示，留着；要复位执行：
   ```sql
   UPDATE mes_wm_batch SET pollution_status=NULL, pollution_location=NULL,
          pollution_marked=NULL, pollution_src_record=NULL WHERE id IN (1003,1004,1010);
   DELETE FROM mes_set_pollution_check WHERE biz_no LIKE 'PS-E2E-%';
   ```
3. `REWORK`（局部缺陷返工）目前只是**标记态**，没有驱动返工工单回路——团队终版设计里「返工回路入追溯链」尚未落到单据。要做需接生产返工单模块。
4. 仓库既有类型错误 7 处（`STAGE_MAP[row.stage]` 一族）本轮未动，属独立清理项。

## 12. 2026-09-10 第四轮：6 张空壳表落成可用模块（一期收口）+ 表注释乱码补扫

承接 §11。本轮处理一期最后一块——**上次审计里被标为「空壳表」的 6 张 `mes_set_*` 表**（有表结构、有菜单占位、没有一行后端代码）。同时补掉一处上三轮的**扫描面遗漏**。

### 12.1 落成的 6 个模块

| 表 | 路由 | 中文 | 菜单 | 错误码段 |
| --- | --- | --- | --- | --- |
| `mes_set_env_report` | `/mes/safety-env/env-report` | 环保检测报告 | 34400 | 1_040_822_xxx |
| `mes_set_emission_outlet` | `.../emission-outlet` | 排放口 | 34410 | 1_040_823_xxx |
| `mes_set_exhaust_gas` | `.../exhaust-gas` | 废气监测记录 | 34420 | 1_040_824_xxx |
| `mes_set_wastewater` | `.../wastewater` | 废水监测记录 | 34430 | 1_040_825_xxx |
| `mes_set_carbon_emission` | `.../carbon-emission` | 碳排放核算 | 34440 | 1_040_826_xxx |
| `mes_set_chemical_safety` | `.../chemical-safety` | 危化品安全检查 | 34450 | 1_040_827_xxx |

每模块 12 个文件（后端 8 + 前端 4），共 72 个，全部由 `codegen/gen_shell_modules.py` 生成——表清单是配置，加表是加一行 `TABLES` 条目。剩下 10 张同构空壳表（`mes_set_dust_record` / `noise_record` / `electrical_record` / `ppe_check` / `pressure_vessel` / `occupational_hazard` / `fire_check` / `gas_record` / `plan` / `standard`）走同一条路，改配置即可。

**菜单号 34400–34454 已全部占用**，二期/三期（危化品链/危废链/排污/应急）**从 34500 起**——原计划的「34400+」作废。

### 12.2 生成器里三个必须踩对的点

1. **枚举选项取自 `SELECT DISTINCT` 真实数据，不取列注释**。§11 之前修过 53 列注释是**人工重建**的（原值字节已丢，见 `rebuild-mojibake-comments-20260910.sql` 头部）。拿重建值反推枚举 = 拿推断当证据。取数据则数据自己说话。
2. **`sn()`（UpperCamel）与 `lc()`（lowerCamel）必须分开**。类名/方法名用 `sn`，**字段名、JSON 属性、TS 属性一律用 `lc`**。第一版没分，Lombok/Jackson 直接暴露成 `ReportNo` / `AuditBy`，且 `id` 因在 `BASE` 里被 `fields()` 排除、RespVO 里根本没声明——e2e 六个模块齐刷刷 `KeyError('id')`。
3. **`id` 是 `BASE` 的成员，生成 DO/RespVO 时要显式补一次**，否则 `obj.getId()` 编译不过。

### 12.3 表注释乱码：前三轮漏掉的扫描面

三轮修复只扫了 `INFORMATION_SCHEMA.COLUMNS.COLUMN_COMMENT`，**`TABLE_COMMENT` 从来没进过扫描面**——所以「498 列全净」是**真的**，但 7 张表的表注释一直是花的。同一类损坏，漏了另一个面。

本轮把两个面都扫了，结果：
- **全库列注释**：仅剩 `_bak_pollution_check_2010_0907`（一次性备份表）23 列，活表全净；
- **全库表注释**：7 张 `mes_set_*` 有乱码，已修（`fix-table-comments-20260910.sql`）。

**方法论修正**（写进脚本注释了）：不能拿 `原串.encode('utf-8').decode('gbk')` 反推比对。`?` 是**丢字节留下的洞**，用 gbk 的 `replace` 反解得到的是 U+FFFD 而不是 `?`，两边永远对不上——我第一版就是这么"验证"的，于是把正确的还原方案判成了错的。正确做法是把**库里存的乱码**逆回字节流（`?` → `0x3F` 占位），再去读那些还完好的字节段。

### 12.4 实测

驱动脚本 `e2e/run_e2e_shell.py`（stdlib，不手写 payload——从库里已有行 get 回来当模板、只换唯一号，字段随表结构自动跟随）。每模块 8 项：建 → 回读 → 撞唯一号被拒 → 缺必填被拒 → 改 → 按号筛选命中 → 删 → 删后查无。

**6 模块 × 8 = 48 项断言全绿**，唯一号冲突错误码逐模块独立（`1040822001` … `1040827001`），经网关 48080、租户 2010。

### 12.5 本轮遗留

1. 另 10 张同构空壳表未落成——加 `TABLES` 条目即可，但**菜单号需另开一段**（建议 34500 已被二期预定，这 10 张可走 34600+）。
2. 表注释命名不统一：老表是 `安全环保检测-xxx`，新表是 `MES SET-xxx`。属历史包袱，不影响功能，未强行统一。
3. 本节只做 CRUD 骨架，**业务规则（如报告与排放口的关联校验、监测数据超标判定）未接**——这些属二期/三期主线（#23）。

## 13. 2026-09-10 第五轮：调度底座 + 入库待检冻结 + 全链重量列（一期收口）

### 13.1 全链重量列

`weight decimal(12,3)` 补进 `mes_set_pollution_check` 与 `mes_pollution_ledger`（`weight-columns-20260910-ddl.sql`）。判定行录入，判为有污染时由台账 builder **单向往台账带入**（`MesPollutionLedgerDO` 注释写明"同一批废物只有一个重量来源"，不做双向同步）。导出表头随之到 23 列，e2e 的 F6 已加 `重量(kg)` 断言。

### 13.2 入库待检冻结：一个投影，五个入口

`mes_wm_material_stock.frozen` 是**推导结果**，不是人工开关。唯一判据 `isBatchFrozen(batch)` 三条取或：

1. 批次污染戳为 `POLLUTED`，或 `pollution_marked`（含 §11 成品整体报废的整批锁定）；
2. 该批**仍有待复核判定行** → 待检期冻结。判 CLEAN 后该行不再是"待复核"，自然解冻；
3. 批次**已过期**。

第 3 条与污染**解耦**，是最容易写错的一处：把过期当成污染的子集，就会"判了无污染"把过期货一起放行。e2e 用 F3c（未过期判 CLEAN → 解冻）与 F5b（已过期判 CLEAN → 仍冻结）成对夹住这个维度。

`syncStockFrozenByBatch(batchNo)` 幂等重算，被 5 处调用：判定行 **create / update / delete**、复核 `applyReviewEffect`、定时任务。其中：

- 复核路径里它**必须排在 `refreshBatchStamp` 之后**——它读的批次污染戳是上一步刚重刷的，顺序反了就会拿旧戳推导，"判 CLEAN 才解冻"会慢一拍解不开；
- 两条 purge 路径（`purgePendingByBizNo` / `...Item`）**删前先记下涉及哪些批次**，删完逐个重算。漏掉这步就是"单据删了、库存永远冻着"——冻结态必须有对应的理由行存在才成立。

### 13.3 定时任务：写法地道，但**本地跑不起来**（必须如实说明）

`MesSetBatchExpireFreezeJob` 按仓库惯例写：`@Component` + `@XxlJob("mesSetBatchExpireFreezeJob")` + `@TenantJob`，薄封装委托给 service 返回 String；`pom.xml` 补上 `txgy-spring-boot-starter-job`。

**但这个任务的"胶水层"本轮没有验证过，也不具备本地验证条件**：

- 本地 profile `xxl.job.enabled: false`，9090 端口无监听，无 docker daemon，仓库内无 xxl-job-admin；
- 即调度中心本身不存在，任务既不会被触发也无法手工触发一次。

所以本轮的绿色只覆盖它**共享的那部分逻辑**（`freezeExpiredBatchStock` 走的还是 `isBatchFrozen` 第 3 条，该判据已被 F5 系列直接打在库真值上验证）。任务的注册、`@TenantJob` 的租户遍历、调度触发**均未跑通**，接入真实调度中心前不能当它已验证。

同理，**没有**加 `@Scheduled` 本地兜底：过期条件本就活在共享判据里，create/复核/delete 三条可验证路径都会重算它，定时扫描只是"没人动过这些批次时"的补漏网，不值得为它引一套只在本地存在的第二条调度路径。

### 13.4 修复：#21 那 6 张表的业务编号唯一键与逻辑删除冲突

见 `drop-code-unique-20260910.sql`（文件头有完整推演）。摘要：

- **现象**：`run_e2e_shell.py` 首轮 48/48 绿，**次轮 6 个创建全 500**——`Duplicate entry 'REP-2026-09-002-E2E' for key 'mes_set_env_report.uk_report_no'`。
- **根因**：唯一键只含业务编号列，删除却是逻辑删除。服务层查重按 `deleted=0` 过滤 → 看不见软删行 → 放行 → INSERT 撞唯一键 → 500。用户界面"建-删-再建同一编号"稳定复现。
- **为什么不写成 `(编号, deleted, tenant_id)`**（仓库里 `mes_wm_misc_issue` 等是这个写法）：`deleted` 是 0/1，所有已删行都等于 1，"建-删-建-删"第二轮删除照样相撞——只是把故障推迟一轮。
- **做法**：去掉这 6 张表业务编号上的唯一键，查重交回服务层（6 个模块都已内置 `*_NO_DUPLICATE`，命中返回业务错误码而非 500，e2e 已断言）。唯一键原本只剩"兜并发建同号"这点价值，不值得用"编号永久不可复用"换。
- **⚠️ 同类隐患未清**：其余 `mes_set_*` 表（`pollution_permit` 的 `uk_permit_no`、`hazardous_waste` 的 `uk_manifest_no`、`plan`/`standard` 等）同样只含业务列，且 `mes_set_pollution_permit` 库里**已有软删行**（id=1、4）。这些不是本轮交付物，逐表确认服务层查重后再处理，未一并改动。

**这轮 48/48 是连着跑两遍都绿的**——上一轮的绿是假绿：它只是史上第一次真正跑过创建步骤（此前几次都在 `KeyError('id')` 就死了），而软删行是"跑过一次才产生"的，所以第一次撞不上。

### 13.5 实测

`e2e/run_e2e_shell.py` **48/48 连续两轮**；`e2e/run_e2e.py` **26/26**；`e2e/run_e2e_freeze.py` **16/16**（本轮新增，每项都读 `mes_wm_material_stock.frozen` 的库真值，不走接口回显）。三个脚本都改为自取 token——mes-server 一重启旧 token 就失效，脚本不该依赖一个会过期的文件。

### 13.6 本轮遗留

1. 定时任务的调度接入（见 13.3），需真实 xxl-job-admin。
2. 13.4 列的其余 `mes_set_*` 表同类唯一键隐患。
3. `REWORK` 仍只是标记态，返工工单回路未接（§11.7 起就记着）；仓库既有的 7 处 `STAGE_MAP[row.stage]` 类型错误未清。

## 14. 2026-09-10 第六轮：排污许可合规（二期「按证排污」主线首刀）

设计文档以 **团队终版**为准（17:42 定稿、自述"合并定稿/单一事实源"；最终版 17:02 为"送评审"稿）。其 §六给出的口径是：**排污许可证 = 全厂环保数据的基准源**——限值取自证、监测对着证、总量卡在证。本轮把这句落成代码。

### 14.1 两条规则，一个权威

`MesSetPermitComplianceService`（`service/set/permitcompliance/`）：

1. **限值比对**：监测行入库时按该排放口的 `permit_limits` 匹配污染物，回填 `limit_value` 并按 `浓度 > 限值 → FAIL` 定 `result`。人工传的 `limitValue`/`result` **不作数**——证是唯一权威（e2e L1 故意传 `limitValue=999`/`result=FAIL`，必须被证上的 30/重算结果覆盖）。浓度**等于**限值算达标（限值是上限，取到边界不算超）。
2. **年总量红线**：自然年 Σ 排放量 ÷ 许可年总量，**≥80% WARN 黄警 / ≥100% RED 红线**，次年 1/1 由自然年边界天然归零。

两个关键取舍：

- **「没配限值」≠「达标」**。`judgeByOutletLimit` 在无排放口/无该污染物限值时返回 `null`，调用方保持原值不动，**不得**编一个 PASS 出来（e2e L4：未配限值的口传 `result=FAIL`，必须原样保留 FAIL，且 `limit_value` 不得凭空生成）。
- **配置期失败快**。`permit_limits`/`annual_limits` 的格式校验放在排放口、许可证的保存口上，而不是等监测入库时才炸——那时整段时间的监测数据都没在比对，**静默失效比报错危险得多**。校验也收紧了：既有 `validateLimitJson` 只检查"是不是一个 JSON 数组"，一串合法但无意义的 JSON 能过检然后在红线判定里静默失效；现改为每条必须 `pollutantCode` 非空且限值 > 0（e2e V2/V3）。

`emission_amount` 与 `annualLimitT` 的**单位**：前者按 kg 记、后者按 t 记，换算系数 `KG_PER_TON` 单独提取成常量并注明——现场口径若是 g/t，改一处即可，判定与红线同源。

### 14.2 演示数据补基准源

租户 2010 此前 `emission_outlet.permit_limits` 全 NULL、`outlet.permit_no` 全空，**有表无基准源**，比对无从谈起。`seed-permit-limits-20260910.sql` 补齐：DA-001 挂 P-110108-2026-0001（颗粒物 30 / SO2 50 / 非甲烷总烃 60 mg/m3），DA-002 挂 P-110108-2026-0002（COD 100 / 氨氮 8 mg/L），两张证补年总量。限值**刻意与既有监测行的 `limit_value` 取同值**，重算回填后不打架。

### 14.3 本轮踩的两个坑（都会静默失败，值得记）

1. **MySQL 的 `||` 是逻辑或，不是字符串拼接**（除非开 `PIPES_AS_CONCAT`）。`'a' || 'b'` 被当数值运算，直接报 `Truncated incorrect DOUBLE value`。JSON 字面量别拼，整条写。
2. **本接口的 `LocalDateTime` 走 epoch 毫秒，不是 `"yyyy-MM-dd HH:mm:ss"` 字符串**。传字符串**不报错**，被静默当 0 → 落库成 `1970-01-01`。后果极具迷惑性：年累计恒为 0、红线永远 NORMAL、接口一切正常。本轮 e2e 的 Q1–Q3 就是这么全挂的，根因不在合规逻辑而在夹具。已加断言 Q0a 专门盯"监测行时间确实落在 2026 自然年"。

第 2 条属于**跨模块的接口约定**，前端按毫秒传所以线上无恙，但任何脚本/联调方按肉眼日期串传都会中招。

### 14.4 实测

`e2e/run_e2e_permit.py` **21/21，连跑两轮全绿**（夹具带 E2E 后缀、跑完物理删除；permit 的 `uk_permit_no` 仍只含业务列，软删会永久占号，故此处必须物理删）。
回归：`run_e2e_shell.py` 48/48、`run_e2e.py` 26/26、`run_e2e_freeze.py` 16/16。**四套合计 111 项全绿。**

### 14.5 本节未做（#23 余量，按设计文档 §六/§七/§八）

- **§六-1 证载管理**：有效期 5 年届满 60 天预警、证载变更留档、逾期未延续锁定作业——依赖调度底座（§13.3，本地仍跑不了）。
- **§六-2 超标后续**：浓度超标"实时报警推应急、关联工单与设施运行、严重时限产停线"，以及 100% 红线**锁定新增作业**——目前只做到**判定与水位计算**，报警推送、工单关联、停限产联动均未接。
- **§六-3 执行报告自动汇总**：`env_report.data_summary` 自动取数、到期建任务、签发回执归档，未做。
- **§七 治污设施**（台账/同开同停/停运申报/换炭→HW49）、**§八 应急**（预案备案/物资演练/泄漏SOP/事件闭环）整块未动。
- 危化品链、危废链、国家联单映射、门卫硬放行、一般固废台账、BPM 审批流、追溯时间轴、环保看板、分表计电：均未开始。
- §13.4 记的其余 `mes_set_*` 表唯一键隐患仍未清。

## 15. 2026-09-10 第七轮：执行报告自动取数汇总 + 证载有效期预警（二期收口前半）

承 §14 的合规引擎往下接。#24 里能本地闭环验的两块：**§六-3 执行报告自动汇总**、**§六-1 的证载有效期预警**。剩下依赖调度/工单联结的部分见 §15.5。

### 15.1 统计期 ≠ 自然年，这是本轮最容易埋的雷

设计文档里这两句话隔了两行，口径完全不同，混起来就会错：

- 行 199/237：执行报告**按许可证载频次**建季报/年报，取数范围是**统计期**。
- 行 236：年累计达 80% 黄警 / 100% 红线，口径是**自然年 Σ**，"次年 1/1 重置"。

所以 `env_report.data_summary` 的汇总里同时存在两种口径，实现上必须分开算、分开标：

| 段 | 口径 | 来源 |
|---|---|---|
| `exhaustGas` / `wastewater` | 统计期 `[periodStart 00:00, periodEnd+1 00:00)` | 直接查监测表 |
| `quota` | 自然年（`periodEnd` 所在年） | 复用 §14 的 `checkAnnualQuota` |

`e2e/run_e2e_report.py` 的夹具就是冲着这条缝设计的：故意放一行**统计期外但同一年**（2026-04-01，777 kg）和一行**跨自然年**（2027-01-05，555 kg）。
断言 A4 要求它不进统计期排放量（500 而非 1277），断言 A8 要求它**进**年累计（1.277 t 而非 0.5）。两边同时成立才算口径没串。

另：统计期取**闭区间**——`periodEnd` 当天 23:00 的行必须计入（夹具 R4），落到 SQL 是 `end.plusDays(1).atStartOfDay()`。

### 15.2 取数内容与"没配就别编"的一贯口径

`data_summary` 结构（`MesSetEnvReportServiceImpl.buildSummary`）：

```
{ periodStart, periodEnd, generatedAt,
  exhaustGas: { records, exceedCount, emissionAmount,
                pollutants:[{pollutantCode,records,exceedCount,emissionAmount}],
                exceeds:[{recordNo,outletId,outletCode,pollutantCode,concentration,limitValue,monitorTime}] },
  wastewater: { 同上 },
  quota:[{outletId,outletCode,pollutantCode,annualLimit,used,ratio,level}] }
```

- `quota` 段对统计期内**出现过的每个 (排放口,污染物)** 都出一条，但**没配年总量时不编水位**：`annualLimit`/`ratio` 为 `null`、`level` 仍 `NORMAL`，只如实报累计量。与 §14.1「没配限值不得凭空造 PASS」同一口径。（断言 A10）
- `exceeds` 里的 `outletCode` 是从排放口表一次性取的映射——原始监测行只存 `outletId`，报告给人看得能认出是哪个口。
- `generatedAt` 与 `monitorTime` 用 `yyyy-MM-dd HH:mm:ss` **字符串**而非 epoch 毫秒。注意这与**接口 DTO** 的约定相反（见记忆 `txgy-mes-api-conventions`）：DTO 走毫秒是框架行为，这里是写给人看的报告快照，故手工格式化。别照抄错边。

### 15.3 建单静默降级、显式调用才报错（有意为之）

`POST /mes/safety-env/env-report/create` 时若统计期已填且调用方没给 `dataSummary`，自动取数写入。但**统计期没填时不报错**——报告本身得能建出来（草稿阶段统计期常常后补）。摘要是空的。

只有显式调 `POST /mes/safety-env/env-report/auto-summary?id=` 才在缺统计期时抛 `1040822004`。
理由同 §14.1 把校验挪到配置保存边界：**静默降级只允许发生在"建单"这一步**，一旦人主动要求补数，缺前提就必须响。断言 C1/C2/C3 把这三态都钉住了。

### 15.4 证载有效期：基准日可传，所以能一次跑完

`MesSetPermitComplianceService.checkPermitValidity(permitId, onDate)`：距有效期止 ≤60 天 `EXPIRING`、已过 `OVERDUE`，`onDate` 缺省取今天。
**到期当天算有效**（`daysLeft==0` → `EXPIRING`，不是 `OVERDUE`）；有效期为空按 `OVERDUE` 处理——证载期限是按证排污的前提，登漏了不能算安全。
因为基准日可传，届满前后各档一次跑完，不用等 60 天：断言 D1(61天→NORMAL)/D2(60天→EXPIRING 边界)/D3/D4(当天)/D5(过期次日→OVERDUE，daysLeft=-1)。
接口：`GET /mes/safety-env/pollution-permit/validity?id=&onDate=`。

### 15.5 本节未做

- **§六-1 剩余**：60 天预警的**定时扫描/待办推送**、证载变更留档、逾期未延续**锁定作业**——扫描依赖调度底座（§13.3，本地跑不了）；锁定作业需要"工单↔排放口/产线"的联结，而 `mes_pro_work_order` **没有**任何 outlet/产线/设施字段（已核对，见 §15.6），无抓手，不硬做。
- **§六-2 超标后续**：实时报警推应急、关联工单与设施运行、严重时限产停线——应急模块与设施台账（§七/§八）都还没有，无处可推。
- **§六-3 剩余**：到期自动建季报/年报任务（调度）、签发回执归档（需要 infra 文件服务依赖，`pom.xml` 里仍没有）。
- **100% 红线锁定新增作业**：红线水位本身已可查（`/annual-quota`，§14；本轮又进了报告 `quota` 段），但"锁定"无处落地，同上——缺工单侧联结字段。

### 15.6 一个核实过的坑：工单侧没有环保联结

`mes_pro_work_order` 的字段是 code/name/type/projectId/productId/quantity×4/clientId/vendorId/batchCode/requestDate/parentId/finishDate/cancelDate/status/remark。
**没有** outlet / 产线 / 设施 / 工序关联列。

所以设计文档 §六 的"关联工单与设施运行（可追溯产线/批次/当时设施是否运行）"和 §七 的"设施未运行→对应工单禁止开工"，在**没有新增联结字段之前是落不了地的**——不是"还没写"，是"没有可写的落点"。谁要做这块，第一步是**先补联结字段**（建议 `mes_pro_work_order` 加产线/工作站关联，或建 `mes_env_facility` 后再挂），而不是直接写校验逻辑。
反过来说，监测行 `mes_set_exhaust_gas`/`mes_set_wastewater` **是有 `wo_id` 的**——工单测没有反向可查，监测侧已具备。

### 15.7 实测

`e2e/run_e2e_report.py` **30/30，连跑两轮全绿**（夹具物理删除；用 2026-03 这段空白期，避开租户 2010 全部落在 2026-09-01 的演示行，计数类断言不被带偏）。
回归四套：`run_e2e_permit.py` 21/21、`run_e2e_shell.py` 48/48、`run_e2e_freeze.py` 16/16、`run_e2e.py` 26/26。**本轮五套合计 141 项全绿**（原四套 111 + 新增 30）。

改动文件：`ErrorCodeConstants`(+1)、两个监测 Mapper(+`selectByMonitorRange`)、`MesSetEnvReportService(Impl)`(+`autoSummary`/自动取数)、`MesSetEnvReportController`(+`/auto-summary`)、`MesSetPermitComplianceService(Impl)`(+`checkPermitValidity`)、`MesSetPollutionPermitController`(+`/validity`)。无新表、无新依赖。

## 16. 2026-09-10 第八轮：接入 infra 文件服务依赖（解开拍照/签名/MSDS/标签的共同前置）

设计文档里需要存文件的地方（拍照、电子签名、MSDS、HJ1276 危废标签、执行报告回执归档）一直缺一个公共底座。本轮把它补上，并且**真跑通了一次跨服务调用**，不是只加了个 pom 依赖。

### 16.1 缺的到底是什么

先说清楚，免得做错方向：**前端上传不需要后端依赖**。拍照/签名/MSDS 这些是前端传 `/admin-api/infra/file/upload`，网关本来就有 infra 路由，后端只需把返回的 URL 存进已有的 `fileUrl`/`signUrl`/`url` 字段。
真正缺 `FileApi` 的是**后端自己生成文件**的场景：危废标签（HJ1276）、报告 PDF、回执归档。

核对结果：mes 全模块 `grep FileApi|createFile` **零命中**，`pom.xml` 无 `txgy-module-infra-api`。缺的确实是这个。

### 16.2 加依赖不是加一行 pom —— 还要注册 Feign 客户端

`@EnableFeignClients` 在本项目是**按模块显式列 clients** 的（`framework/rpc/config/RpcConfiguration.java`），不做包扫描。所以只加依赖会编译过、启动过，但注入 `FileApi` 时找不到 bean。
参照 system/fms/mp/ai 的写法，在 mes 已有的 `RpcConfiguration` 里补上：

```java
@Configuration(value = "mesRpcConfiguration", proxyBeanMethods = false)
@EnableFeignClients(clients = {AdminUserApi.class, PostApi.class, RoleApi.class, DictDataApi.class,
        FileApi.class})   // ← 新增
```

改了两个文件：`txgy-module-mes-server/pom.xml`（+infra-api）、`framework/rpc/config/RpcConfiguration.java`（+FileApi）。

### 16.3 把 infra-server 跑起来（本地拓扑新增一个进程）

infra-server 此前**从未在本地构建过**（`.m2` 里没有 jar），也没在跑。要验证 Feign 真通，必须让它起来：

```
mvn -q -pl :txgy-module-infra-server -am install -DskipTests
java -jar .../txgy-module-infra-server-2026.07-jdk25-SNAPSHOT.jar --spring.profiles.active=local
```

端口 **48082**，nacos 注册名 `infra-server`。起来后 nacos 服务列表 = `mes-server / system-server / infra-server / gateway-server`（4 个）。

顺带核实了一件事：**本地 nacos 配置中心是空的**（`/nacos/v1/cs/configs` 查询 totalCount=0），所有服务配置都来自各自 jar 内打包的 `application-local.yaml`。所以"配置在 nacos 上"这个直觉在本地不成立，改配置就是改源码里的 yaml 再重新打包。

文件存储落在库里：`infra_file_config` 的 master 行是 **id=4「数据库（示例）」**（`storage=1` DB 存储），`infra_file` 存文件内容/元数据。归档出来的 URL 形如
`http://127.0.0.1:48080/admin-api/infra/file/4/get/mes/env-report/20260910/xxx.json`，**经网关、GET 无需鉴权**，可直接取回。

### 16.4 第一个真实用点：执行报告归档

接 §15 的 `data_summary`，加 `POST /mes/safety-env/env-report/archive?id=`：把报告摘要落成 `.json` 存进文件服务，URL 回写 `file_url` 并返回。
- 摘要为空时先自动取数（复用 §15 的 `buildSummary`）；统计期也没有 → `1040822004`，**不产出空壳文件**。
- 文件服务不可用/返回异常 → `1040822005`。这条是刻意加的：报告标成"已上报"却取不回文件，比当场报错危险得多。
- 用 `file_url` 存生成件，与字段原注释"第三方导入PDF"语义一致——都是"这份报告的文件"，只是来源不同。

它同时是 `FileApi` 的**首个真实调用方**，所以验证不是"注入成功"这种弱证据，而是整条链路：mes-server → nacos 发现 → infra-server → 落库 → URL 可取回且内容对得上（断言 E0–E5）。

### 16.5 实测

`e2e/run_e2e_report.py` **38/38**（新增 E 段 8 项），连跑两轮全绿；`infra_file` 里本脚本造的归档行也已随夹具清理（`DELETE FROM infra_file WHERE name='EP-E2E-RPT-0901.json'`），跑完复查计数为 0，不脏共享表。
回归四套：`run_e2e_permit.py` 21/21、`run_e2e_shell.py` 48/48、`run_e2e_freeze.py` 16/16、`run_e2e.py` 26/26。**五套合计 149 项全绿。**

### 16.6 后续怎么用

拍照/签名/MSDS 前端直传即可，后端只需存 URL，无需改动。
后端生成件的场景（**HJ1276 危废标签**、报告 PDF）现在可以直连 `FileApi`：注入 `FileApi fileApi` → `fileApi.createFile(bytes, name, directory, type)` 返回 URL。目录建议按业务分（本轮用 `mes/env-report`；标签建议 `mes/hw-label`）。
**仍未做的**：标签/PDF 的渲染本身（等 #26 危废链与固化表单落地），以及 `infra_file` 的清理策略——目前归档文件只增不删。

## 17. 2026-09-10 第九轮：危废链（台账 + 国家固废转移联单五方 + 门卫硬放行 + HJ1276 标签）

### 17.1 先说清楚缺口在哪

`mes_set_hazardous_waste` **表早就在库里**（还有 3 行演示数据），但**一行代码都没有**——没有 DO、没有 Service、没有菜单、没有前端。所以这一轮的工作量不在建表，在于把这个模块真正落成。同时它还是 §13.4 点名的"同类隐患"：业务编号上挂着唯一键，和逻辑删除互斥。

处理沿用 §13.4 定下的口径，不搞特殊：
- 拆掉 `uk_manifest_no`，只留普通索引；
- 新建的 `mes_set_hazwaste_manifest` 从一开始就只给普通索引，并在 DDL 里写明原因；
- 查重挪到 Service 层，撞号返回业务错误码 `1040829001`，而不是抛 500。

### 17.2 门卫为什么必须是"硬"的

设计文档 §194 的原话是：**「门卫扫车牌/联单码硬校验：联单号存在+已生效+四方签字齐，缺一不放行」**。四条校验逐条落地，每一条给一个独立错误码，而不是笼统的"校验失败"：

| 校验 | 错误码 | 语意 |
|---|---|---|
| 联单号存在 | `1040829007` | 拿不出联单，后面三条免谈 |
| 联单已生效 | `1040829008` | 已申报 且 运输方、接收方均已确认 |
| 车牌一致 | `1040829009` | 签字齐不能替车牌；两边任一为空则跳过核对 |
| 三方签字齐 | `1040829010` | 移交环保员 / 押运司机 / 接收经手人 |

`gate-check` 是纯校验（给门卫看"为什么不让走"），`gate-release` 才放行。**关键点是拦截必须拦在写库之前**：e2e 里每条拦截都额外查了一次库，确认联单状态还停在「已生效」——只返回 `passed:false` 而库里已经放出去了，那才叫真的没拦住。

放行成功四件事在一个事务里：补签门卫（四方会签的第 4 方由放行动作补上）→ 联单转「已出厂」→ 写 `transferTime`/`gateReleaseTime`/`gateGuard` → 同单号台账推到「转移」。**复扫已出厂的单是回放不是重放**：仍返回放行信息，但放行时间一个字不改（e2e C9 前后对拍）。

### 17.3 生效判据对齐国家系统的"三方确认"

联单按 `草稿 → 已申报 → 已生效 → 已出厂 → 已归档` 走。生效的判据不是"申报了就算"，而是 **产生方已申报 且 运输方、接收方都已确认**，三条齐了才在 `confirmParty` 里自动翻状态。

顺序也是硬的：e2e B1 先确认运输方、再申报，确认那次被记下但不生效；B3 只差接收方一方，仍停在「已申报」；B4 补上接收方当场转「已生效」。**申报不是"点一下"，它是生效的前置条件**，顺序反了就真的反了。

### 17.4 只进不退：两条单调性

`STAGES = 产生→贮存→转移→处置`，`MANIFEST_STATUSES = 草稿→已申报→已生效→已出厂→已归档`。两条线都做单调性校验，回退和原地重复推进都拒（`1040829004` / `1040829006`）。

理由写在代码注释里：**危废台账是负法律责任的流水，退一格就是改历史**。配套两条：
- `updateHazardousWaste` 显式把 `stage` 置空——改单不许改环节（e2e A7 传 `stage=GENERATED` 进改单接口，库里仍是「处置」）；
- 已出厂 / 已归档的联单，前端整块字段转只读（出厂信息是追溯证据）。

### 17.5 危险特性：没配就别编（第三次应用同一口径）

`HW_HAZARD_TRAITS` 只收设计文档锚定过的四个码（HW06/08/12/49），**没收录的一律留空，不猜**。这跟 §15.2 取数、§12 判据是同一条纪律。

标签上写错一个危险特性，比留空危险得多——留空会被看见，写错会被当成真的。UI 侧配了一句提示，让人按现行《国家危险废物名录》人工补录。e2e 双断言：HW49 带出「毒性」，HW99 必须是 `null`。

### 17.6 复用而不是新建

| 需要的 | 没做什么 | 用了什么 |
|---|---|---|
| 四方电子签字 | 不再建一套签名表 | `mes_set_sign_record`（`bizType = HAZWASTE_MANIFEST`），只给 Mapper 加了个 `selectListByBiz` |
| 五方确认的载体 | 不再开第二个列表页 | 直接锚在台账行上，一个页面看全 |
| 前端看签字明细 | 不新写接口 | 复用现有 `getSignRecordPage` |
| 标签二维码 | 不引新依赖 | `@vueuse/integrations` 的 `useQRCode`（web-antd 的直接依赖） |

### 17.7 本轮抓到的一个真 bug

`closeManifest`（回执归档）原来只做 `目标序号 > 当前序号`，于是 **「已申报」能一步跳到「已归档」**——跳过整个出厂环节，凭空生出一份没有出厂记录的回执。但它的错误提示写的是"只能按 草稿→已申报→已生效→已出厂→已归档 逐级推进"，**代码和自己的提示语对着干**。

是 e2e C12 逼出来的。修法很窄：归档只认 `TRANSFERRED`。顺带一个级联——C12 修好前 M2 被误关成 CLOSED，导致 E0（倒排提醒）查不到它而假失败；修完两处一起绿。

**台账侧的 `to > from` 保留不动**：小厂漏记「贮存」直接记「转移」是真实存在的，前进跳格是录入便利，回退才是改史。这两处的松紧不一样是有意的。

### 17.8 实测

`e2e/run_e2e_hazwaste.py` **43/43**，五组：A 环节单调（9）、B 五方生效（9）、C 门卫硬放行（13）、D 标签（7）、E 倒排提醒（1）+ 清理（4）。

标签归档真走了 mes → nacos → infra 全链路，URL 取回的内容里能查到这一桶的废物名称与容器码（不是"存了个字符串"就算数）；夹具里的 `infra_file` 行跑完清理复查为 0。

回归五套：`run_e2e.py` 26/26、`run_e2e_freeze.py` 16/16、`run_e2e_permit.py` 21/21、`run_e2e_report.py` 38/38、`run_e2e_shell.py` 48/48。**六套合计 192 项全绿。**

新菜单 34390–34395，新角色 60467「门卫」+ 演示账号 `huahan_menwei`（只给 34000/34390/34391/34395，够扫码放行，够不着改单）。

### 17.9 本节未做

- **危化品链**（五双管理 / 分级储存 / 效期 FIFO）——#26 的另一半，尚未动。
- **标签渲染本身**：本轮把 HJ1276 的字段集归档成 JSON（目录 `mes/hazwaste-label`，与 §16.6 设想的 `mes/hw-label` 不同，以实际为准）。真正的 PDF/图片渲染没做。
- **国家固废系统对接**：`declareManifest` 只是本地状态推进，**没有真的往国家平台发报文**——这是"预留接口"不是"已对接"，用的时候要清楚。
- **地磅/电子秤直采**：`net_weight` 字段留了，手工填。
- **倒排提醒的推送**：`/manifest/due-soon` 是个可查的待办列表（含已逾期），**不是定时推送**——本地没有调度底座可依（见 §13.3）。

## 18. 2026-09-10 第十轮：危化品链（档案 + 分级储存禁配 + 储量上限 + 五双双人签字）

对应任务 #26 的另一半。第九轮（§17）做的是危废，本轮做危化品——两者同属设计文档《MES环保模块质优化设计-最终版-2026-09-09》的"危险品全生命周期"主线，但**管控对象完全不同**：危废管的是"出厂之后去哪了"，危化品管的是"进场之后放在哪、放多少、谁能动"。

### 18.1 缺口在哪

设计文档 §313 把危化品链排在 `chemical_safety` 这张空壳表上。**但那张表的语义不是档案**——它已经是"一次巡检的快照"（`label_ok` / `msds_ok` / `storage_ok` / `separation_ok` / `result` / `inspector` / `inspect_time`），是"某天某人去查了一次，结论合格不合格"。

而本轮要的是**物料本身的常量属性**：这个料属于哪个相容组、该进哪个专区、储量上限多少、同库位能不能和它放一起。这是"一物一行、很少变"，跟"一天一行、天天变"不是一回事。硬塞进巡检表，就会出现"改一次档案=伪造一次巡检记录"。

所以新建 `mes_set_chemical_profile`，与巡检表用 `chemicalCode` 关联。理由写在 DDL 抬头和 DO 的 javadoc 里，免得后来人以为漏了。

字段：`profile_no / chemical_code / chemical_name / cas_no / compat_group / hazard_class / storage_zone / storage_location / storage_limit / storage_unit / stock_quantity / incompatible_groups / explosion_proof / msds_url / msds_expire_date / expire_manage / shelf_life_days / emergency_measure / status`。

**建表就只给普通索引**：`profile_no` / `chemical_code` / `storage_location` / `compat_group` 四个 `KEY`，没有 UNIQUE。这是沿用 §13.4 定下的口径——业务编号唯一键和逻辑删除天然冲突（建→删→再建就撞 500），查重交给 Service 层返业务错误码（`1040830001`）。

### 18.2 四道校验的顺序不是随便排的

`checkStorage` 按 **① MSDS 挂载 → ② 专区要求 → ③ 同库位禁配 → ④ 储量上限** 出结论，`stockIn` 按同一顺序逐条抛具体错误码：

| 序 | 判据 | 不过的错误码 |
|---|---|---|
| ① | 已挂载 MSDS | `1040830003` |
| ② | 须防爆的料在防爆区 | `1040830004` |
| ③ | 同库位无禁配物料 | `1040830005` |
| ④ | 现有 + 本次 ≤ 上限 | `1040830006` |

顺序的含义是**"连进来的资格都没有"排在"进来多少"前面**：没有 MSDS 的危化品根本不该出现在厂里（设计文档原话：无 MSDS 的危化品不下采购单），这比"你这个库位放不下"更靠前。库管看到的第一条原因，应该是最该先去解决的那条。

**④ 只在给了本次入库量时才判**：`quantity` 为空时 `quotaOk` 返回 `null`，而不是 `true`。"没给量"和"给了量、量没超"是两件事，混成同一个 `true` 会让前端把"没校验"显示成"校验通过"。e2e 的 B6 专门钉这一条。

### 18.3 五双必须按**账号**判重，所以加了一列

设计文档 §5.3 要求"双账号各签一次"，§295 又写"账号即签名"。但 `mes_set_sign_record` 当时只存了 `sign_user`——**一个给人看的昵称**。

拿昵称判重是错的：两个人重名、或者同一个人改了昵称，都会判错。判错的后果是两个方向都严重——要么放行了一个人签两次的假双人，要么拦住两个真的不同人。

加 `sign_user_id`（`ALTER TABLE mes_set_sign_record ADD COLUMN sign_user_id bigint`），`doubleSign` 按它判重，撞了抛 `1040830007`。历史行为 NULL（那时没有双人判重需求），判重时只认非空值。三个写入点（`signManifest` / `releaseGate` / `doubleSign`）都补了赋值。

五双的五个动作（`RECEIVE` / `KEEP` / `LOCK` / `ISSUE` / `TRANSPORT`）**各签各的**，互不干扰——e2e D5 钉的是"收发货签齐了，领料还能重新从一开始签"。

### 18.4 禁配只对**同库位**判，不搞全场一刀切

禁配矩阵只收设计文档 §5.2 / §130 点名的组合：

```java
{"ISOCYANATE","POLYOL"},  // 黑料与白料剧烈反应 → 分间、双锁分开
{"ISOCYANATE","WATER"},   // 异氰酸酯遇水放热剧反应
{"ISOCYANATE","ALCOHOL"},
{"ISOCYANATE","AMINE"},
```

判定时只比**同一 `storage_location` 上已启用的其他档案**（`selectEnabledByLocation`），不是全库扫描。理由：禁配讲的是"放在一起才反应"，黑料在 A 间、水在 B 间是安全的，全场一刀切会拦掉本可以并存的两批货。e2e B3 专门放了一桶单独库位的水，它必须通过。

矩阵之外还有 `incompatible_groups` 人工追加列（逗号分隔），给"文档没写但厂里知道"的组合留了口子——**默认空，不猜**。

### 18.5 没配就别编（与 §17.5 同一口径）

`GROUP_NAMES` 只收 7 个文档点名的物料族（黑料/白料/稀释剂/环氧粉末/水/醇/胺），没收录的**按原值回显**，不猜中文名。前端的下拉也只给这 7 个。

`EXPLOSION_PROOF_GROUPS` 只有 `THINNER`——文档写了"稀释剂：易燃液体专柜"，别的易燃品文档没点名，就不替它做主。真需要时走 `explosion_proof` 布尔字段手工勾。

`getAlerts` 只出三类：**超量 / MSDS 缺失 / MSDS 将到期**。**刻意没做批次效期预警**——那已经是 wm 侧冻结投影的判据③（§13），再做一份就是两处判同一件事，早晚对不上。

### 18.6 复用而不是新建

| 想做的事 | 实际做法 |
|---|---|
| 五双签字存哪 | 复用 `mes_set_sign_record`（`bizType = CHEMICAL_DOUBLE`），不新开表 |
| 查某单某动作的签字 | 复用 `signRecordService.getSignRecordPage`，没加 Mapper 方法 |
| 取操作人姓名 | 复用 `SecurityFrameworkUtils.getLoginUserNickname()` |
| MSDS 文件存哪 | 复用 §16 接好的 infra 文件服务 |
| 存量怎么算 | 就是档案上的 `stock_quantity` 计数，由 `stockIn` 累加，**不建库存账**——真正的批次/FIFO 在 wm 侧（§5.3） |

`updateProfile` 里有一行 `updateObj.setStockQuantity(null)`：改档案不许覆盖存量。**那是账，不是属性**——档案表单里根本没有存量字段，防的是有人绕过前端直接 PUT。

### 18.7 本轮撞出来的一次菜单 id 事故（我的锅，记下来）

危化品菜单原本排在 `34396~34402`。但 **`34400` / `34401` / `34402` 早就被「环保检测报告」占了**（`shell-modules-menu-20260910.sql:12-14` 是它的父菜单/查询/新增）。该脚本的 `DELETE ... BETWEEN 34396 AND 34402` 把环保报告的这三行**连它的角色绑定一起删了**，还把环保报告剩下的 `34403`/`34404`（修改/删除）挂到了一个已经变成"按钮"的 `34400` 下面。

**表面症状完全不像菜单问题**：`huahan_anquan` 在 `mes:set-chemical:check` 和 `:sign` 上返 403，但在 `:query` 上正常。查下来是权限链取 `menu_role_ids:2010:34401` —— 拿到的还是**环保专员 60466**（旧绑定），不是安全员 60468。

修法（`chemical-20260910-menu-fix.sql`，已应用）：
1. 环保报告 `34400~34402` 按原定义恢复，`60185` / `60466` 的原绑定一并恢复；
2. 危化品这 3 个按钮挪到**空段 `34460~34462`**（`34455~34499` 经查未被占用）；
3. `34396~34399` 无冲突，保持不动。

原 `chemical-20260910-menu.sql` 也改了，并在抬头加了显式警告，免得重跑再撞一次。

**两个教训**：
- 排菜单 id 前**先查 `system_menu` 实际占用**，不能只信同批脚本的注释（危废脚本的抬头写着"34400+ 留给设计稿 dg_* 新页"，但环保报告其实已经先占了 34400~34404）。
- 清了 Redis 菜单/权限缓存**还不够**——mes-server 自己还握着一份**内存里的**权限缓存。删了 `menu_role_ids:2010:34401` 之后重新请求，这个 key **没有重新写入**（说明走的不是 Redis），403 照旧。**必须重启 mes-server**。

### 18.8 实测

新增 `e2e/run_e2e_chemical.py` **31/31**，五组：
- A 档案 CRUD（4）：编号重复 `1040830001` / 非法状态 `1040830002` / 不存在 `1040830000`
- B 分级储存四道校验只判不写（7）：含"不传入库量时 `quotaOk` 为 null"
- C 入库硬拦截（8）：**每条都额外查库确认存量没动**——`C0` 拦了之后 `stock` 仍是 `0.0000`
- D 五双双人签字（7）：含"同一账号再签 → `1040830007`"与"两个不同账号 → `complete=true`"
- E 预警汇总（3）+ 清理（2）

五双那一组用两个真实登录账号：`huahan`（管理员/库管，80370）与 `huahan_anquan`（危化品安全员/李敏，80934，角色 60468）。**安全员刻意不给建/改/删档案权限**——他能签字，但不该能改档案本身。

前端：`api/mes/safetyEnv/chemical/index.ts` + `views/mes/safetyEnv/chemical/{data.ts,index.vue,modules/{form,check-modal,double-sign-modal,alerts-modal}.vue}`。

回归七套：`run_e2e.py` 26、`run_e2e_freeze.py` 16、`run_e2e_permit.py` 21、`run_e2e_report.py` 38、`run_e2e_shell.py` 48、`run_e2e_hazwaste.py` 43、`run_e2e_chemical.py` 31 —— **合计 223 项全绿**。其中 `run_e2e_report.py` 正是环保报告的接口，绿灯证明 18.7 的修复没留下后遗症。

### 18.9 本节未做

- **危化品出入库的真实单据流**：`stockIn` 是"过校验后给档案计数加一笔"，**不是一张入库单**。真正的收发要挂采购/领料单据，本轮没接。
- **效期 FIFO**：`shelf_life_days` / `expire_manage` 字段留了，按效期出库的顺序没做（在 wm 侧）。
- **五双的审批流**：签字是**事后留痕**，签齐了不自动放行任何东西，也不进 BPM。
- **国家平台对接**：与 §17.9 同一句——预留，未对接。
- **应急模块**：应急措施只有 `emergency_measure` 一个文本字段，没有应急预案/演练/物资（见 #27）。

---

## 19. 二期：治污设施运行管控（台账 / 换炭→HW49 / 停运申报审批 / 同开同停）

设计文档 §6.3。菜单 34463~34468（`治污设施`，父 34000）。权限键 `mes:set-facility:query|create|update|delete|operate`。
`operate` 一个键管换炭/停运申报/停运审批/复运四个动作——它们都是"对设施动手"，拆开只是多配四个键。

### 19.1 表名：`mes_set_treatment_facility`，不是设计稿的 `mes_dg_facility`

沿用 §17.1 的结论：设计文档里的 `mes_dg_*` 前缀在仓库里**不存在**，全部是 `mes_set_*`。建表脚本抬头写明了这处偏离。

### 19.2 三条管控都是"绕不过去"，不是"提醒一下"

这一节的全部价值在于：**每条约束都堵死在结构上**，而不是前端弹个提示。

**① 换了炭不能不记危废。**
`replaceConsumable` 是 `@Transactional`：登记废活性炭 HW49 与顺延下次更换日期在**同一个事务**里。换下的活性炭**就是** HW49，写不写危废不是操作员的选择。
- 危废行是**复用 `MesSetHazwasteService.createHazardousWaste`** 建的，不是自己拼 DO 插进去——编号查重、默认环节/状态全走危废链那套口径。
- 量 ≤ 0 直接拒（`1040831006`）。**不放行 0**：放行等于给危废台账留一条空行，"记了"和"没记"就分不清了。

**② 申报 ≠ 停运。**
`declareShutdown` 只把 `shutdown_status` 置 `PENDING`，**设施仍是 `RUNNING`**。只有 `approveShutdown(approved=true)` 才置 `STOPPED`；**驳回时 `run_status` 回到/保持 `RUNNING`**——驳回的设施还在转，这是两回事。
审批动作写一条 `mes_set_sign_record`（`biz_type='FACILITY_SHUTDOWN'`），复用签字表白拿一份审批历史，不另建审批表。

**③ 改档案绕不过停运审批。**
`updateFacility` 显式把 `runStatus` / `shutdownStatus` / `shutdownDeclaredAt` / `shutdownApprover` / `shutdownApprovedAt` **置 null** 再更新。
理由写在了代码注释里：运行状态与停运申报不是"档案属性"。否则改一次档案就等于绕过了审批，停运审批也就白设了。e2e 的 F 组专门打这一条——用 update 接口塞 `runStatus=RUNNING, shutdownStatus=APPROVED`，库里纹丝不动。

**④ 复运顺手归档。**
`resumeFacility` 回到 `RUNNING` 并把 `shutdown_status` 清回 `NONE`。不清的话，下次要停运时 `declareShutdown` 的 `PENDING` 判定会拿着上一轮的历史挡路。

### 19.3 同开同停：产线运行信号**由调用方给**，系统不编

设计文档 §6 想的是"设施未运行 → 工单禁止开工"。查过了：**`mes_pro_work_order` 表没有产线 / 设施 / 排放口列**，自动取不到这个信号。

选择是把 `productionRunning` 做成**入参**，并在 VO 上写清楚为什么——**没配就别编**。编一个假的产线状态挂上去，比不挂更危险：它会让"同开同停"变成一个显示绿色但对不上现实的灯。

校验是**只读**的，违规时返回两条分开的原因：
- `同开同停：产线「X」运行中，配套治污设施 Y(Z) 处于停运，产能不能开`
- `未批先停：该设施停运申报状态为 NONE（须已批准）`

两种违规性质不同，分开说。产线也停了则不算违规（停机检修是正常的，不能一刀切）。
`stopped-without-approval` 是一个**独立清单接口**：已停运但申报不是 `APPROVED` 的设施，供巡检直接捞。

### 19.4 "没周期就不算到期"

`calcNextReplace(base, cycleDays)` 在 `base` 或 `cycleDays` 缺失时返回 **null**，不拿建档日凑。
`next_replace_date` 是派生值（上次更换 + 周期），所以**不放进表单**——手填只会填出一个跟换炭日志对不上的日期。表单里连字段都没有。

### 19.5 实测

新增 `e2e/run_e2e_facility.py` **24/24**，六组：
- A 台账 CRUD（4）：编号重复 `1040831001` / 非法类型 `1040831002` / 不存在 `1040831000` / 新建默认 `RUNNING`+`NONE`
- B 换炭→HW49（4）：**B0 额外查库确认 `mes_set_hazardous_waste` 真多了一行**；B1 核对行的字段（名称/数量/容器码/环节=GENERATED）；B2 日期顺延 90 天；**B3 量填 0 被拦后台账没多行**
- C 停运申报审批（7）：未申报不能审批 `1040831004` / 重复申报 `1040831003` / **申报后仍 RUNNING** / 驳回后仍 RUNNING / 批准才 STOPPED / 签字表留痕 / 复运归档
- D 同开同停（4）：通过 / **未批先停同时点出两条原因** / 产线也停则不算违规 / 未批先停清单
- E 换炭到期（2）：10 天后到期在窗口内、**逾期 5 天也在**（逾期不能漏）
- F 改档案绕不过审批（1）：见 19.2 ③
- 收尾清理（2）

> B1 一开始报失败：我把期望数量写成 `120.0000`，实际是 `120.000`——`quantity` 列是 `decimal(12,3)` 不是 `(12,4)`。是**断言写错**不是代码错，改期望值即可。

前端：`api/mes/safetyEnv/facility/index.ts` + `views/mes/safetyEnv/facility/{data.ts,index.vue,modules/{form,replace-modal,shutdown-modal,co-run-modal,due-modal}.vue}`。

> 写前端时踩了一个自己的坑：`co-run-modal` / `due-modal` 里用的是 **ant-design-vue 的 `Table`**（不是 vxe），列定义该用 `dataIndex`，我按 vxe 的 `field` 写了，`vue-tsc` 报 `Property 'field' does not exist on type 'ColumnType<any>'`。两处 `bodyCell` 的 `column.field` 也一并改成 `column.dataIndex`。

### 19.6 本节未做

- **同开同停没挂到工单上**：校验接口有了，但工单开工时不会自动调它——因为工单表没有产线/设施列（见 19.3）。要真联动，得先给工单补一列产线/设施，那是改 `mes_pro_work_order` 的事。
- **停运申报没有超期提醒**：申报后一直不审就一直是 `PENDING`，没有"待审批超过 N 天"的告警。
- **换炭到期没接调度**：`due-replace` 只在页面点开时查，没挂 XXL-Job 定时推（与 §17.9 / §18.9 同一句：调度胶水未验）。
- **同开同停只在设施侧判**：没有"一台设施停运 → 反查影响哪些产线"的反向视图。

**回归**：八套全绿，**合计 247 项** —— `run_e2e.py` 26、`run_e2e_freeze.py` 16、`run_e2e_permit.py` 21、`run_e2e_report.py` 38、`run_e2e_shell.py` 48、`run_e2e_hazwaste.py` 43、`run_e2e_chemical.py` 31、`run_e2e_facility.py` 24。（§18.8 记的 223 是当时七套的基线。）

## 20. 2026-09-10 事故与修复：生成器无参数重跑冲掉第一批 6 个模块的手写代码

### 20.1 发生了什么

为了给第二批 10 张空壳表生成模块，跑了 `python gen_shell_modules.py`——**没带表名**。
而 `main()` 里是这句：

```python
picked = [t for t in TABLES if not args or t["table"] in args]   # 没参数 = 全选
```

`TABLES` 当时已有 16 张表，于是**第一批 6 张（env-report / emission-outlet / exhaust-gas / wastewater /
carbon-emission / chemical-safety）被原样重写**。生成器只会写 CRUD，§14~§16 长在这几个文件里的业务逻辑
被整体抹平。

关键教训有两条，都不是"要小心"这种废话：

1. **冲掉的代码编译期不一定报错，运行期不一定抛异常。** 这一轮实际被冲掉 5 处，症状各不相同：

   | 被冲掉的东西 | 症状 |
   |---|---|
   | 两个监测 Mapper 的 `sumEmissionAmount` / `selectByMonitorRange` | **编译不过**（`PermitComplianceServiceImpl` 调不到）——唯一一个当场暴露的 |
   | 监测 Service 的 `applyOutletLimit` | 编译过、接口正常，只是 `limit_value`/`result` 不再按证重算——**静默失效** |
   | 报告 Service 的 `autoSummary` / `archiveSummary` | 编译不过（Controller 在调） |
   | 排放口 Service 的 `validateOutletLimits` 调用 | 编译过、接口正常，非法限值 JSON 照收——**静默失效** |
   | 排放口 Controller 的 `GET /annual-quota` | 接口 404，但没有任何编译期线索 |

   **所以"能编译过"完全不能作为"没被冲掉"的证据。** 这次是靠 e2e 才发现的。

2. **`.m2` 里的 fat jar 不是可靠的恢复源。** 我一度用它当基线做 `diff_jar_vs_src.py`，但它只在
   "最后一次成功 `install`" 时更新——重跑一次构建就被覆盖成新的（含冲掉之后的）内容。本轮就是这么
   丢掉基线的，最后是靠 **e2e 断言 + 文档 §14~§16 的改动清单**把行为规格还原出来的。
   临时副本**没有 `.git`**，写下来的文档就是唯一的历史。这条以后每次都要记住。

### 20.2 怎么修的

按"文档写了什么 + e2e 断言要什么"反推，逐处补回（不凭记忆编）：

- `MesSetExhaustGasMapper` / `MesSetWastewaterMapper`：`selectByMonitorRange(start, end)`（闭起开止）
  与 `sumEmissionAmount(outletId, pollutantCode, start, end)`。
- `MesSetExhaustGasServiceImpl` / `MesSetWastewaterServiceImpl`：`applyOutletLimit`，在 create/update
  里调 `permitComplianceService.judgeByOutletLimit`；**返回 null 就原样保留人工值**
  （e2e L4a：未配限值的人工 `FAIL` 不得被改成 `PASS`——「没配」不等于「达标」）。
- `MesSetEnvReportService(Impl)` + `Controller`：`autoSummary` / `archiveSummary` 与
  `buildSummary/buildSummaryQuietly/section/quotaSection/concat` + 内部 `record Mon`，
  外加 `POST /auto-summary`、`POST /archive`（两个都复用 `mes:set-env-report:update` 权限——
  从 jar 的注解常量池里核过，不是新编的）。
- `MesSetEmissionOutletController`：`GET /annual-quota`。
- `MesSetEmissionOutletServiceImpl`：`validateBase` 里补回 `permitComplianceService.validateOutletLimits`。

统计期/自然年两种口径、闭区间边界、`dataSummary` 字段形状全部按 §15.1/§15.2 与
`run_e2e_report.py` 的 A/B/C/E 段断言还原，没有一处是"看着差不多"写的。

### 20.3 顺带清掉的假故障

修完第一次回归，permit 的 V1/V2 与 report、shell 的收尾断言红了。查下来**不是代码问题**：
在"校验被冲掉"的那段时间里跑过 e2e，V 组那两个**本该被拒**的排放口
（`DA-E2E-BADJSON` / `DA-E2E-BADITEM`）真的落库了；而 V 组的设计前提是"必然被拒，所以无需清理"，
清理逻辑自然不会删它们。它们又恰好是 shell 套件取模板时的"最新一行"（`page?pageSize=1`），
于是把坏 JSON 当模板复制，连累了一个跟排放口无关的套件。

物理删掉这两行残留后 8 套全绿。**教训：e2e 的"必然失败"型夹具一旦真的成功，会留下没人负责清理的脏行**，
而这类脏行会顺着"取最新一行当模板"这种写法污染别的套件。

### 20.4 堵住根因（这是本轮真正的产出）

改了两处，让同一件事**不可能再发生**，而不是靠"记得带参数"：

1. **不给表名就拒绝执行**（`gen_shell_modules.py:main`）。全量重写要显式写 `--all`，
   且建议先 `--dry` 看会碰哪些文件。

2. **`write()` 只覆盖"还是生成器上次写的样子"的文件**。生成器落盘的内容用 sha1 记在
   `codegen/.gen_manifest.json`；要覆盖时先比对当前内容——**对不上就说明有人手改过，拒绝并提示**。
   首次遇到"已存在但没有记录"的文件同样拒绝（宁可不写，也不猜它是不是手写的）。
   `--force` 才强行覆盖。

   实测：`python gen_shell_modules.py mes_set_env_report` 现在会逐文件打印
   `[skip] ... 与生成器上次写的对不上（有人手改过），拒绝覆盖`，源码一字未动。

   > ponytail: 用 sha1 全量比对而不是行级 diff，够用且最短；将来若需要"只重生成某几个方法"，
   > 再说那是另一个工具的事。

### 20.5 实测

- 修复后 8 套回归：**247/247 全绿**（26+16+21+38+48+43+31+24），与 §19.6 的基线一致。
- `codegen/diff_jar_vs_src.py` 已不再可靠（基线 jar 被覆盖），保留作"与某个已知 jar 比成员名"的工具，
  **不要再把它当恢复源**；真正的恢复源是 e2e + 本文档。

### 20.6 顺带把第二批 10 张空壳表验了

第二批 10 张表（粉尘/噪声/气体/电气/消防/劳保/压力容器/职业危害/检测计划/检测标准，菜单 34600~34690）
此前只有"生成了、编译过了"，**没有一条断言跑过**。它们与第一批 6 张是同一套生成器产物，
所以直接把 `run_e2e_shell.py` 的 `MODULES` 列表从 6 条扩到 16 条，同一套 8 条断言跑一遍
——生成器以后改坏一处，这里能同时照出 10 张表。

扩的时候踩了一个**夹具的坑，值得记**：第 4 条负例"缺必填被拒"要挑一个真 NOT NULL 的列，
生成器的 `@NotEmpty` 是按 `IS_NULLABLE='NO'` 推出来的。我第一版给粉尘/噪声/气体/电气/消防/压力容器
六张都挑了 `location`（压力容器挑 `vesselRegNo`），而这两列都可空 → 负例永远"不被拒" → 6 条红。
**那是夹具错，不是校验漏**——换成 `dustType`/`sourceType`/`gasType`/`checkItem`/`facilityName`/`inspectOrg`
后全绿。跟 §19.5 的 `decimal(12,3)` 一样：断言先怀疑自己，再去怀疑代码。

**回归（新基线）**：`run_e2e_shell.py` **128/128**（原 48，+80 = 10 张表 × 8 条），
八套合计 **327 项全绿** —— 26+16+21+38+**128**+43+31+24。
